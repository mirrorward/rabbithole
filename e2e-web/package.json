{
  "name": "rabbithole-e2e-web",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end smoke test for the RabbitHole web SPA served by `burrow --http`.",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.ts",
    "test:theme": "playwright test tests/theme.spec.ts",
    "test:routes": "playwright test tests/routes.spec.ts",
    "test:keepalive": "playwright test tests/keepalive.spec.ts",
    "test:chat": "playwright test tests/chat.spec.ts",
    "cleanup": "node scripts/cleanup-fixtures.mjs",
    "test:keynav": "playwright test tests/keynav.spec.ts",
    "test:pwa": "playwright test tests/pwa.spec.ts",
    "test:radio": "playwright test tests/radio.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
