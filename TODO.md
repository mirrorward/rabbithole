# RabbitHole — Implementation Tracker

**Client customization (2026-09-24):** personal appearance controls with live OS
appearance, persistent chat typography/density/timestamps, global server-theme
opt-out and High Contrast protection; configurable chime voices, volume and
preview. List navigation leaves nested input editing keys alone.
macOS View/Go/Help menus target only the main window, with app-local history;
native menu actions dismiss the command palette and preserve skip-link history.

> Distilled from `PLAN.md` (read that first — it has the specs, rationale, and
> dependency graph). Check items off as they land. Waves must respect the
> dependency edges shown in PLAN.md §15. ⛔ = do not start until PLAN.md is
> reviewed and approved by the project owner.

**Adversarial review pass (0.130.0–0.133.0)**: a 50-agent workflow (5 risk-dimension
finders × 3 skeptic verifiers each, majority-vote) reviewed the ~3500 lines of this
session's work (native swarm backend + verified-key wire path + identity/welcome/
persistence). It surfaced **13 confirmed bugs** (all 3/3 consensus) and correctly
**dismissed** 2 false alarms (incl. "plaintext seed in localStorage" — fine for a
browser identity). All 13 fixed:
- **Honesty (HIGH)**: the People "verified ✓" overclaimed auth for a server-*echoed*,
  unsigned, spoofable pubkey → relabeled to a neutral self-asserted identity-key hint
  (⚿, "not verified"); You-hub copy reframed. Real verification (Ed25519 challenge/
  response, the existing KEY_AUTH cap) is the honest follow-up.
- **Swarm (HIGH/MED)**: `.rhstate` poison → self-heal (remove state+dest on verify-fail);
  `size==0` wiped partials → guard before `set_len` + at the caller; stalled-peer hang →
  `PEER_READ_TIMEOUT` on header+body reads.
- **Native routing (HIGH/MED/LOW)**: progress folded into the *focused* session → route
  by `transfer_id` (`AppState::transfer_session_files`); fully-resumed download never
  Done → `Done`→terminal `TransferOpened`; failed invoke hung at 0% → mark Failed.
- **UI/security (HIGH/LOW)**: stale Directory dot (`<For>` key += `online`); presence
  ranking (Online>Idle>Away>Invisible); `sanitize_name` rejects `:` (Windows drive/ADS);
  postcard additive-field forward-compat caveat documented.
Regression tests added at each layer.

**Ed25519 key-auth — the review's headline follow-up — landed (0.134.0–0.136.0)**, in 3 slices:
the identity key is now *cryptographically verified* across the whole stack, so the People ✓
is earned, not claimed.
- **Slice 1 (proto)**: `HelloAck.challenge: Option<[u8;32]>` (additive) + a new `KeyProof
  { signature }` message (SESSION type 3, REGISTRY 179→180, golden reblessed).
- **Slice 2 (server + core)**: on a `Hello` with `client_pubkey` the server issues a random
  OsRng nonce + advertises `KEY_AUTH`; a `KeyProof` is Ed25519-verified over the nonce;
  `SessionCtx.pubkey` is the **verified** key only (a claim without a valid proof is never
  surfaced). Core `connect_with_identity` takes `Option<&IdentityKey>` and signs the
  challenge (identity moved to a `native`-gated optional dep so it stays out of the wasm SPA).
  E2E: `who_list_carries_the_verified_pubkey` (happy path) + `unproven_claimed_pubkey_is_not_
  surfaced` (a raw malicious client claiming a victim's key with a garbage proof gets NO key).
- **Slice 3 (web)**: `identity::sign`; `ws.rs` answers the `HelloAck` challenge with a signed
  `KeyProof`; the People mark is the honest accent ✓ ("verified identity · fp") again.
  Verified live end to end (`alice ✓`, tooltip fingerprint == the You hub's).
The impersonation vector the review found is closed: possession is proven, not assumed.

**⚠ Key-auth is possession-proven but NOT relay-proof (deep review, 0.141.0).** A focused
5-surface adversarial review found the domain-separation fix (0.137.0) did **not** stop the
relay it claimed to: `server_key` is self-asserted by the peer in `HelloAck` and bound to
nothing (not the TLS/QUIC cert; over browser WS the cert is invisible to JS). Live relay: a
malicious burrow A the user connects to opens its own session to honest burrow B, echoes B's
`server_key`+nonce to the user as A's challenge, collects the signed `KeyProof`, forwards it
to B → B marks A's relayed session as the victim's key. Confirmed 3/3 by 5 agents; 8 weaker
findings correctly dismissed. **Immediate fix (0.141.0)**: removed the false "verified ✓" —
the People mark is now a neutral identity-key hint (⚿ "possession proven, not relay-proof");
You-hub + `key_auth_message` doc state the limitation. The challenge/response stays (it stops
a *passive* pubkey-copy and is the foundation for a real fix). **Channel binding — DONE (0.142.0).** The real fix landed: the proof now binds to the
transport channel (`key_auth_message` = `"rabbithole-key-auth-v2" || channel_binder || nonce`,
where `channel_binder` is the server's TLS cert fingerprint of *this* connection).
- **QUIC: relay-proof.** The native client binds to the cert fingerprint it pinned when
  dialing; the server binds to its own cert fp. A relayed proof (signed for burrow A) fails at
  burrow B (different cert fp). Tested both ways: `quic_client_with_pinned_fingerprint_is_verified`
  (honest path verifies) + `proof_over_wrong_channel_binder_is_rejected` (a *valid* signature
  over the wrong binder — exactly a relay — is rejected).
- **WS: possession-only** (`NO_CHANNEL_BINDING` = [0;32]) — the browser can't read the TLS cert,
  so it stays not-relay-proof; honestly labeled (no false ✓). Kept the UI single-tier +
  conservative (all keys shown as the possession hint), so QUIC under-claims rather than adding
  a two-tier UI for a mostly-web userbase. A future strong-✓ tier (surface channel-bound-ness in
  the who-list for QUIC peers) remains possible but wasn't worth the proto+UI surface now.

The relay finding is fully resolved: defeated where technically possible (QUIC), honestly
disclosed where not (WS).

**Key-auth hardening + reach (0.137.0–0.138.0)** — NOTE: the "verified" framing in the entries
below was corrected to "possession-proven, not relay-proof" in 0.141.0 (see above):
- **Relay/oracle fix (0.137.0)**: a background security review flagged that all three
  sides signed/verified the *raw* nonce — a signing oracle that let a malicious burrow
  relay an honest burrow's challenge to a connected user and impersonate them as
  "verified" elsewhere. Fixed with **domain separation**: everyone now signs/verifies
  `hello::key_auth_message` = `b"rabbithole-key-auth-v1" || server_key || nonce`. Binding
  to the domain (never a signature over arbitrary bytes) + the server key (a proof for A
  fails at B) kills the relay. Proto test asserts the properties; e2e still agrees.
- **Native CLI identity (0.138.0)**: `rabbit` now persists an Ed25519 seed
  (`dirs::data_dir()/rabbithole/identity.seed`) and presents it via
  `connect_with_identity` on login/reconnect, so CLI sessions carry a verified key too.
  Verified live (login completes the challenge/response; seed stable across logins).
  **Both real client families — web SPA + native CLI — now present a cryptographically
  verified identity.** (The TUI is a terminal demo; left handle-only by choice.)

---

## Session status (as of key-auth + native swarm arc)

The original ask — desktop/mobile multi-server client with centralized status, unified
cross-server downloads, and multi-source swarming (servers **and** peers) — is built and,
where headless-verifiable, tested:
- **Multi-server warren client** (web SPA): rail, unified status/People/Transfers, portable
  verified identity, welcome sheet, reconnect-on-launch.
- **Native swarm backend** (Tauri desktop): multi-source Bao-verified downloads wired end
  to end (6 slices + live-Burrow E2E); the only unverified link is the wasm↔Tauri IPC
  round-trip, which needs `cargo tauri dev` on a real machine.
- **Verified identity** across proto/server/web/CLI, hardened against relay.
- **An adversarial review** (13 fixes) + a security-review relay fix.
- **You page redesign** — mark hero with a picker (16 faces x 8 colours, persisted,
  honestly labelled local-only), copyable fingerprint, public key behind a
  disclosure, and what the key does as four plain cards.
- **Failed transfers say why, and retry** — the reason rides on the transfer (keyed
  by id, not "the most recent"), sources-tried is shown, Retry resumes from the
  .rhstate, and sources-per-download is a Settings slider.
- **Looking Glass is live** — pulls rabbithole.directory (HTTPS JSON, CORS-open),
  falls back to a tracker's INDEX line protocol via the native shell, and labels
  which source answered.
- **Identity backup/restore** — export a recovery document (seed + public key +
  fingerprint, self-warning), restore it with a tamper check; About window spacing,
  in-place copy, real links; the meaningless "Home" rail tile is gone.
- **About window** — a themed SPA route in its own window (not the system panel),
  reporting the workspace version + git SHA stamped at build time; the binary is
  named RabbitHole so Command-Tab says so.
- **macOS shell** — the app menu is "RabbitHole" (not the binary name), with
  About RabbitHole (icon + description) and Settings… on Cmd-, routed into the SPA;
  Edit/Window rebuilt so copy/paste survives; the Dock/Command-Tab icon is a proper
  rounded macOS icon (`scripts/build-macos-icns.sh`).
- **The logo, everywhere the system shows the app** (0.215.0) — a Mac dev run is a real
  `.app` now (`scripts/macos-dev-app.sh`, wired in as cargo's runner in
  `apps/desktop/.cargo/config.toml`), so Activity Monitor, Force Quit and every other
  process list show the logo instead of the generic "exec" tile; the Windows `.ico`,
  the Linux PNGs and the iOS/Android sets were still a placeholder and are rebuilt from
  the brand masters (`scripts/build-app-icons.sh`); the About window shows the logo
  itself, not a drawn stand-in.
- **Settings + demo warren + chrome cleanup** — a Settings page (trackers, default
  `tracker.rabbit.direct`), two named demo burrows behind a dev-only `demo` feature
  (news on connect, distinct rosters), Leave a burrow, header trimmed to the burrow's
  real name, You first in the warren nav with a "me" icon.
- **Person page + signed friendship** — `/people/:seed` shows where you know someone
  from (persisted sightings ledger, survives disconnect), their handle per burrow,
  recent DMs, files they've shared, and a mutual Ed25519 friendship attestation
  (both halves required; offers travel as self-authenticating DMs).
- **Keyboard lists + motion at native tempo** — ↑/↓/Home/End drive every list
  (container is the single Tab stop; rows tabindex=-1; modifiers pass through),
  one 28px control height in the title bar, palette pop-in, pip pop on arrival
  only, fullscreen reclaims the title strip (with initial-state sync).
  **RH-2 focus recovery**: a removed focused row moves to its nearest surviving
  neighbor, or the empty list; filtering, explicit blur, focus handed to another
  control, and route disposal never steal focus. A real-browser regression drives
  the live Members list. **RH-3**: Boards also has one Tab stop; arrows traverse
  its real links, with Enter, pointer and modified-click navigation preserved.
- **The rail is one icon family** — rings (home/brand), smiley (people), arrow-into-
  tray (transfers), bust (you), plus — same 20px box, same stroke, contact-sheeted.
- **Native-feel pass** (50-agent audit, 20 confirmed findings) — real Tauri drag
  regions (the -webkit-app-region CSS was a WKWebView no-op: the window was
  undraggable), no browser context menu over chrome, window frame remembered
  across launches, drawn bell/file/rail icons replacing emoji, themed presence
  select, pressed states on every row, instant pane switches, global scrollbar
  styling, single focus ring, double-click downloads, steady dropzone highlight,
  palette selection stays visible, OS-matched pre-boot. Deferred: arrow-key list
  navigation, fullscreen strip reclaim, title-bar control height token.
- **The stale-webview class is closed** — the PWA service worker never runs in the
  native shell (and purges poisoned installs); html/body repaint with the theme bg;
  the SPA stamps its version into the DOM and the shell's diag line reports it.
- **Sidebar only when the rail item has rooms** — a burrow shows its sections;
  People/Transfers/You/Servers are single screens with no subnav.
- **Header controls are icons** — the server-theme opt-out button is gone (a burrow's
  theme always applies); appearance and light/dark are icon buttons.
- **Scoped navigation** — the rail picks a scope (a burrow, or the warren) and the
  sidebar lists that scope's sections; the active rail destination highlights.
- **Native feel** — macOS overlay title bar (the app header *is* the title bar),
  window drag region, no rubber-banding or tap flash, unselectable chrome with
  selectable content, and Cmd-1..9 section shortcuts.
- **Rich text** — a formatting bar + markdown mode over chat, DMs and board posts,
  rendered on display through a written, host-tested markdown renderer that escapes
  first and allowlists link schemes. Posts now declare `text/markdown` on the wire.
- **Warren marks are drawn, not generated** — sixteen 8x8 sprites (rabbit, fox, owl,
  ghost, crown...) assigned from your identity, replacing the procedural blobs.
- **Left section sidebar** — the nav moved out of the header (nine destinations plus
  title, status, radio, ⌘K, presence and theme in one row ran off the window) into a
  vertical sidebar with icons, and a fixed bottom tab bar on phones.
- **Burrow news** — the server's `WelcomeScreen` renders as a permanent panel at the
  top of the lobby (it used to be nested inside the dismissible welcome sheet, so a
  burrow with no MOTD showed no news at all). The sheet is now agreement-only.

**Needs the user's machine (can't verify headlessly here):** running the desktop app
(`cd apps/desktop && cargo tauri dev`, watch the console for the `[rh-native]` bridge
lines), and the mobile builds (iOS simulator / Android NDK).

**Status: Waves 0–5 complete (swarm coordinator, Bao-verified peer wire, multi-source scheduler, resumable fetches, blob cache policy; NAT traversal + WebRTC deferred to their proper environments). Wave 6 (legacy surfaces) underway — the `art` (CP437/ANSI/SAUCE), `legacy-telnet` (negotiation + login shell), and `legacy-finger` (RFC 1288) crates have landed as standalone libraries; Wave 10 gained an RSS/Atom parsing crate. Next: wire these surfaces into `burrow`.**

> W4.2: transfers are resumable + integrity-checked, folder-pipelined, and
> move bytes over dedicated QUIC bulk streams (off the control channel) with
> a WebSocket ranged-chunk fallback — one protocol, one verification path.

---

## Wave 0 — Foundations
*Depends on: — (first wave)*

- [x] Cargo workspace scaffold: all `crates/*` + `apps/*` stubs compile
- [x] CI: fmt + clippy + test matrix (Linux/macOS/Windows) + wasm build check
- [ ] Licensing (⛔ owner still deciding — see PLAN §16); README ✅, CONTRIBUTING ✅, rustfmt/clippy config ✅
- [x] `proto`: RHP frame (version/kind/family/type/id/error/payload), postcard framing, error model
- [x] `proto`: version negotiation + capability flags; `#[non_exhaustive]` discipline
- [x] `docs/protocol/` skeleton — spec is a deliverable
- [x] `identity`: Ed25519 keygen/sign/verify; Argon2id (m=64MiB,t=3,p=1, PHC, rehash-on-login)
- [x] `identity`: opaque session tokens; TOTP (RFC 6238) + hashed recovery codes
- [x] `net`: `Transport` trait; quinn (QUIC/TLS1.3) impl; tokio-tungstenite (WS) impl
- [x] `net`: rustls setup; ACME (`rustls-acme`) + self-signed w/ fingerprint pinning
- [x] Storage: `Repository` traits; sqlx/SQLite (WAL) + rusqlite skeletons; migration harness
- [x] Content-addressed blob store (blake3 pathing, refcount GC)
- [x] `core` / `server-core` skeletons: Command/Event API, event bus (tokio broadcast)

## Wave 1 — Vertical slice: a server you can talk to
*Depends on: W0*

- [x] Server daemon: QUIC + WS listeners; graceful shutdown
- [x] Hello/HelloAck; auth: password, guest (toggleable), token resume; keepalive; reconnect w/ replay cursor
- [x] Roles (guest→superuser) + classes + u64 capability bitmask
- [x] ACL evaluator: nearest-ancestor, deny-wins, hide-vs-deny, cached effective masks (property-tested)
- [x] Presence registry actor; who's-online query + pushes
- [x] Public chat (lobby), server agreement gate, MOTD
- [x] `rabbit` CLI: login, who, chat, JSON output mode
- [x] `burrow ctl`: config get/set, account create, local admin socket
- [x] Config system: TOML + env overrides + hot-reload-where-safe
- [x] `tracing` + audit-log skeleton — operator read surface added: ctl `audit-log {limit?, actor?, action?}` over `AuditRepo::recent` (recent-first window, optional actor/action filters); 1 e2e

## Wave 2 — Community layer
*Depends on: W1*

- [x] Registration gating (open/invite/email), TOTP enrollment, key enrollment
- [x] Class admin (create/edit/assign; live inheritance)
- [x] Personas: multiple per account (cap configurable), switcher
- [x] Profiles (location/interests/quote/free text/.plan), avatars + **banner images** (blob-backed, size-capped)
- [x] Member directory + search; "locate online" (privacy-gated)
- [x] Buddy lists: server-stored, groups, permit/deny; presence states (online/away+msg/idle/invisible) + pub/sub pushes
- [x] Chat: multiple public rooms w/ categories + topics; ad-hoc rooms; private rooms w/ invite/join/leave; subjects; room kick/ban (mute + slow-mode landed in W13 moderation hardening — see the Wave 13 moderation entry) **Keeping a room from the app (0.262.0)**: mute, unmute, slow mode and kick/ban were answered on the wire and offered nowhere. `RoomModerationRequest`/`RoomModeration` (CHAT 28/29) say how a room is kept: its pace, whether the asker may keep it, who is in it (to keepers only, without anybody invisible unless the keeper is a moderator; `RoomMembersRequest` now keeps the same rule), and who is muted (every mute to a keeper, only your own to anybody else; a private room to nobody outside it). A mute is named by the name it was given in that room, never by another of the account's names. Somebody who does not keep a room takes a mute or a pace change from the push itself instead of asking again, so a lobby of hundreds does not all ask at once. Under the room bar everybody is told the pace and a muted person until when (a time of day, so it is never stale; the room is looked at again when it runs out, since a burrow lets a timed mute lapse without a word); the room's maker and chat moderators get "Keep this room": the pace, and each person with Mute (10 minutes, an hour, a day, until lifted), Let them talk, Take out and Take out and bar, asked twice. Every refusal of a room — a message refused for a mute or slow mode, a keeper turned down — is said in the scrollback in words for what was tried; being taken out of a room brings you to the lobby and says so there. Found on the way: rooms were joined only when nobody else was in them (the list's member count was read as "you are in it"), so anything said in a room with somebody already there was refused; and the scrollback was keyed by line number alone, so switching rooms could show the room you had left. A connection that drops rejoins the room being read, or goes to the lobby if the room is gone. **Every pane asks again on each sign-in (0.268.0)**: panes asked the burrow for what they show only when they opened, so after a burrow restart or a dropped socket they kept showing what was true before — and anything asked for in flight died with the socket. `AppState::each_sign_in` asks now and again whenever `Session::ready` moves (bumped on every `Event::Authenticated`), deferring the later asks a tick because a sign-in is announced from inside the socket’s own sink. Twenty-odd loads moved onto it: Lobby rooms, Boards, DMs (guest-ness read at ask time), the member list, Files and Art areas and upload limits, Radio, Wishes, and every console pane (areas, backups, boards and their keeping, peers and origins, invites, stations, held, deny list, audit, sessions, and reports for whichever state is being read). Live: a snapshot added on disk while the burrow was down showed up in the open Backups pane on reconnect, and eight panes rode a restart with no panic. **Recent room history (RH-143)**: web and desktop sessions fetch scrollback after sign-in/reconnect and room opening, correlating replies to their room and connection and merging them with live messages. Live, history and replay keep the same server timestamp; same-tick messages receive distinct ordered timestamps so repeated identical text survives reconciliation. Still open: new rooms and joins are not pushed, so the room strip and a keeper's list of people are as fresh as the last look; a mute of a guest outlives the guest until the room goes.
- [x] DMs: threads, offline queueing, quoting, away auto-response, receipts (privacy-gated)
- [x] DM attachments (server-config max size, content-addressed)
- [x] Notifications: protocol pushes + client-side sounds (optional, tasteful)
- [x] Welcome screen composer (widgets: MOTD, unread, who, featured, ticker)
- [x] Server theme bundle v1 (signed, content-addressed: logo, banner art, accent tokens, icon set) in welcome bundle
- [x] Keyword registry + `/go` fuzzy teleport
- [x] TUI client v1: login, chat, who, DMs (`screen` crate begun); light/dark palettes
- [x] Server TUI v1: connection monitor, config, accounts
- [x] Remote admin protocol family live (capability-gated + audited)

## Wave 3 — Message bases & offline
*Depends on: W2*

- [x] Board hierarchy (categories/bundles/boards, dotted slugs) + per-board ACLs + moderators
- [x] Posts as signed blake3 events (author + server sigs) — federation-ready
- [x] Threading (parent/root), markdown/plain/ANSI bodies, edit-as-event, tombstones
- [x] Retention/auto-archive policy (KDX-style)
- [x] Per-user read pointers; unread counts surfaced (welcome, keyword bar)
- [x] Client offline store: board cache, batch delta download (content-id merge), offline read/reply, outbox sync on reconnect
- [x] Request system ("wishing well"): CRUD, voting, claim → fulfilled linkage, requester notifications
- [x] Shared dupe/seen subsystem (time-windowed, multi-ID-form) + tests
- [x] CLI/TUI board reading + posting; `rabbit sync`/`read`/`reply`/`wish`

## Wave 4 — File libraries & transfers
*Depends on: W3*

- [x] Areas + folder trees; metadata: icons (retro set + custom), comments, uploader, dates, download counters, ratings
- [x] Aliases; **drop boxes** (write-only, privilege-gated viewing); hide-vs-deny folder ACLs
- [x] Background file indexer → instant search (projection-backed substring search; FTS5 later)
- [x] Transfer engine: ticketed resumable transfer with dedicated QUIC bulk streams (WS ranged-chunk fallback), whole-file blake3 verify; per-chunk Bao verify shares the W5 swarm crate
- [x] Folder transfers (pipelined, no per-item lockstep) — one FolderManifest round trip, then independent per-file transfers (`rabbit file getdir`)
- [x] Quotas + rate policy — per-account upload storage quota (`upload_quota_bytes`) at ticket + inline upload; per-account transfer **concurrency cap** (`max_concurrent_transfers`, refused with `RateLimited`) with session-scoped ticket cleanup; per-transfer **bandwidth cap** (`transfer_rate_bytes_per_sec`) on both download paths (per-class overrides deferred)
- [x] Persistent client transfer queue: priorities + auto-resume across restarts (store-client `transfer_queue`); queue driver (`rabbithole_core::queue::drain`, bandwidth cap via `Client::set_rate_limit`) + CLI `rabbit queue get/put/list/run/pause/resume/prio/rm/clear`
- [x] CLI file browse + transfer UX (`rabbit file areas/ls/put/get/search/rate/…`); TUI + big-file transfers with W4.2

## Wave 5 — Swarm ("the warren")
*Depends on: W4*

- [x] Spike: iroh vs quinn+custom for hole punching/relay → **decision: quinn + custom** (stack is already quinn end-to-end with fingerprint pinning and the coordinator handles discovery; iroh is the documented fallback if NAT traversal underdelivers) — see docs/protocol/swarm.md
- [x] Manifest format (per-file blake3 roots, 1 MiB chunks) + `rabbit://` links — `rabbithole-swarm` `Manifest`/`ManifestFile` (content-addressed id = blake3 over canonical postcard bytes; path-sorted for determinism) and `RabbitLink` (`rabbit://host[:port]/{files,manifest,blob}/…`, percent-encoded, root-pinned). CBOR interop deferred to a later slice.
- [x] `AdvertiseFiles` (list-without-upload): metadata catalog, permission scopes, TTL soft state + re-announce — SWARM family (6) types 1-5, `SwarmCatalog` (TTL'd soft state, per-account cap `swarm_adverts_max`, session-scoped cleanup), gated by `SWARM_ADVERTISE` on the `swarm` resource; `rabbit swarm share/find/unshare`
- [~] Coordinator: FindSources (scope-gated, reports origin-server fallback + source count as list-level rarity) done; per-chunk rarity annotation arrives with the peer wire/scheduler
- [x] Server-signed capability tokens; peer-side verification — `rabbithole-swarm::CapToken` (ed25519 over `rhp-swarm-cap-v1`-separated claim {root, fetcher, expiry}), issued via `SourceTicketRequest` (FILE_DOWNLOAD-gated, 10 min TTL), verified against the hello-time server key; `PeerContact` cards (observed-IP + declared port + pinned cert fp) join `SourceList` entries
- [x] Peer wire over QUIC: request/response with Bao-verified streams — `rabbithole-swarm::peer` (PeerServer on the rabbithole-net QUIC stack, one bi-stream per `PeerRequest{token, root, offset, len≤4MiB}`, responses are Bao streams at 16 KiB chunk groups verified block-by-block against the root; SeedStore precomputes outboards and refuses root-mismatched files; `rabbit swarm share` seeds / `rabbit swarm fetch` does find→ticket→fetch). **Partial seeding (0.229.0)**: fetches keep each verified unit's Bao proof in `<dest>.obao`; `SeedStore::add_partial` serves the units held (validated against their proofs, `NOT_HELD` otherwise); an empty-frame `PeerAsk::Have` returns a `HaveMap`; the scheduler takes each unit from a source that holds it and re-asks partial seeds; `fetch_swarm_sharing` shares while fetching and ends as a whole seed from the kept proofs; partial seeds are advertised apart (`AdvertisePartial`, SWARM 9) and found only with `FindAllSources` (SWARM 10), so older clients never meet one, and no session is its own source; the desktop app advertises a download that way once its first verified unit lands, and falls back to the burrow when peers cannot finish (not on a local disk error). **Burrows as chunk sources (0.230.0)**: `RangeSource` in the engine (sources hand over Bao streams, `fetch_proved` checks them against the root); the burrow serves proved ranges of stored files under a download ticket (`ProvedRangeRequest`/`ProvedRange`, FILE 43/44, outboards built in the background and cached as `<blob>.obao`, `Unavailable`/`BUSY` until ready) and joins the desktop app's swarm downloads beside the peers (burrows reporting 0.230 on); the sending burrow of a server-to-server send answers `PullStreamAsk::Range`, paced per file, and is mixed with its seeders at the destination as one source with four lanes (a send with no seeders stays plain). **Per-burrow sessions (0.231.0)**: the desktop app holds each burrow's native session behind its own lock, so a download no longer blocks another burrow's download, the re-announce pass, or the Settings screen (a burrow busy downloading is skipped, not waited for). **Finding content across burrows (0.232.0)**: `FileByContentRequest`/`FileByContent` (FILE 45/46) answers which file here holds a blake3 root, for a download running on another burrow — the checks a download ticket makes minus the ticket (deny list and quarantine first, then `FILE_LIST` + `FILE_DOWNLOAD` per path and the drop-box rule, walked a page at a time past copies the asker may not have), guests refused, charged to the transfer budget, `NotFound` alike for "not here" and "not yours". **Other burrows as sources (0.233.0)**: the app asks its other sessions which of them holds the content (FILE 45/46), takes a ticket from those that do, and they carry units beside the peers — `BurrowSource` drives its own session per ask (2s to get a turn, else the unit goes elsewhere and the burrow keeps its place), `other_burrows` picks them (Auto only, never the origin, deduped by server key, 0.232 on, at most four), and what they lend is never advertised back (`RangeSource::shareable`, `FetchReport::borrowed`). **The command line too (0.234.0)**: `BurrowSource` and the other-burrow rule moved to `rabbithole-swarm::burrow` (behind the `client` feature, so a serving burrow does not build them), and `rabbit swarm fetch` takes the burrow as a source — it asks which file holds the content (FILE 45/46), so a fetch works with no peers at all instead of telling the person to use `file get`. **Accepting reaches the burrow (0.240.0)**: `AppState::dispatch` drives the seeded demo seam, not the live socket, so the accept added in 0.239.0 went nowhere on a real session; `accept_agreement()` sends it over `focused().ws` like `send_chat` does. Clicked in the running app against a live burrow to be sure: the sheet appears, the line is refused before, and lands after. **What the second review found (0.239.0)**: the app now sends `AgreementAccept` (its "Accept & enter" only closed the sheet, so the burrow was never told and asked again every reconnect); the agreement gates joining in rather than chat alone (one check inside `SessionCtx::allows`, so looking works and doing does not until it is accepted); stopping a download aborts the fetch instead of detaching it, and gives every ticket back on the way out; a burrow that refuses a range keeps its ticket for the fallback; sharing switched off mid-download wins; the re-offer pass re-reads the setting and does its file reading off the runtime; one download per transfer at a time; `shared.json` is written beside and renamed over. **One file, one download (0.238.0)**: a swarm attempt that falls back to the burrow's own stream hands on the ticket it already opened (`Client::transfer_download_with`), so the file's count moves once and one transfer slot and rate token are spent, not two. **Sharing outlives the app (0.237.0)**: what this machine offered each burrow is noted beside the download preferences (`shared.json`, at most 200 files a burrow, the same content once), and offered again in the background when that burrow is signed in to next — each file read to check it is still what was shared, and quietly forgotten when it is not. Switching sharing off forgets the lot. Still not seeded: a file saved through the webview's own socket (a guest cannot advertise, and a signed-in person's downloads take the native path, which does). **What the review found (0.236.0)**: "Peers only" no longer asks the person's other burrows anything (the mode is settled before any of them is touched); lent units are remembered in the resume record (`RhState::borrowed`) so a resumed or retried download still never offers them on; a burrow whose session is busy answers `STATUS_BUSY` rather than "not held", so it keeps its place instead of being struck off after eight turns; helper tickets are given back on every exit, and a close no longer takes the ticket out of the books when the session cannot be had; a missed turn no longer ends partial sharing for the rest of a download; a failed row says how many sources were tried. **Stopping a download (0.235.0)**: the shell keeps a stop switch per running download (`swarm_cancel_download`), the fetch is dropped where it stands so what was verified stays on disk, and the row says "Stopped." and offers a retry that resumes; the Transfers Cancel button reaches it through the native bridge
- [x] Multi-source scheduler: per-source speed assignment via work-stealing (1 MiB units, faster peers naturally pull more), endgame duplication of in-flight stragglers, failed sources retire and their units migrate — `rabbithole-swarm::scheduler::fetch_swarm`; `rabbit swarm fetch` uses all reachable sources. Rarest-first is a cross-file ordering (coordinator source counts) and lands with manifest-set fetching
- [x] Server chunk cache policies (none/LRU/mirror) — blob store `evict_unreferenced_over(max_bytes)` (oldest-first, referenced library content never evicted) driven by a periodic `maintenance` task on `swarm_cache_max_bytes` (0 = unlimited/mirror; positive = LRU cap; policy notes in config)
- [~] NAT: hole punching + server relay fallback; optional UPnP/NAT-PMP; "relay-only" privacy mode — **port-mapping landed** (`rabbithole-portmap`): golden-file-tested NAT-PMP (RFC 6886 ext-addr + MAP UDP/TCP), PCP (RFC 6887 MAP, 24B header+36B payload, v0/v2 byte), UPnP-IGD (SSDP M-SEARCH + AddPortMapping/DeletePortMapping/GetExternalIPAddress SOAP builders + tolerant response parser) codecs — total decoders, 29 tests; a best-effort async `PortMapper` (NAT-PMP→PCP over UDP, bounded timeouts/retries, proven to return `NoGateway` cleanly against a silent gateway — never hangs/panics). Opt-in burrow startup (`portmap_enabled` default off, `portmap_gateway` router LAN IP, `portmap_lifetime_secs`) fire-and-forget maps QUIC+WS, never delays/fails boot. Still deferred (need a real multi-NAT network): direct hole-punching, relay/TURN fallback, relay-only mode; also live UPnP control-URL discovery + routing-table gateway auto-discovery. `fetch_range` keeps its bounded `PEER_CONNECT_TIMEOUT`
- [x] `.rhstate` persistence — resumable swarm fetches (`fetch_swarm_resumable`: unit bitfield persisted atomically per unit, foreign/stale state ignored, whole-file hash check on completion catches lying partials, state removed on success; `rabbit swarm fetch` resumes). Bao-outboard persistence + partial *seeding* (serving what you have) landed in 0.229.0 (see the peer wire line above)
- [ ] WebRTC gateway for browser peers — deferred to W8 (browser transport lands with the wasm SPA)
- [x] Multi-peer simulation test harness (lossy links, corruption injection) — `crates/swarm/tests/sim.rs`: 10-peer swarm, flaky/dead/wrong-fingerprint majority, and inline corrupting/truncating malicious peers proving an adversary can waste time but never land a wrong byte; surfaced+fixed a real `QuicListener::accept` bug (one bad handshake no longer kills the listener for all peers)

## Wave 6 — Telnet BBS + finger + art pipeline
*Depends on: W2, W3 (W4 optional for file menus)*

- [x] `art` crate: CP437↔Unicode tables, ANSI/SGR + cursor parser, iCE colors, renderers to terminal/plain/**PNG**/**HTML** — `rabbithole-art` (`cp437`/`ansi`/`sauce`/`render`/`raster`/`font`, `AnsiParser`→`Canvas` of `Cell`; PNG thumbnails via embedded 8x16 CP437 VGA font + 16-color palette; `<pre>`+`<span>` HTML output; fuzz-tolerant; 73 tests). Live ANSImation streaming deferred
- [x] SAUCE reader/writer (128-byte record + COMNT) — `rabbithole-art::sauce`, tolerant reads / strict writes, iCE-color tflag, roundtrip tests
- [x] `screen` crate: CP437/ANSI + UTF-8 text surface — `rabbithole-screen` (direct `Cell` buffer over `rabbithole-art`, `ScreenMode::{Utf8,Cp437Ansi}`, box/menu drawing with reverse-video selection, SGR-coalesced `flush`; no ratatui dep). Socket wiring into the telnet shell is the next slice
- [x] Telnet codec: IAC state machine, ECHO/SGA/NAWS(resize)/TTYPE, 0xFF doubling, loop-safe negotiation — `rabbithole-legacy-telnet` (`proto`/`negotiate`/`stream`, sans-IO parser + RFC 1143-style state machine, line IO w/ password mode, CP437/UTF-8 seam); BINARY option TBD with the art integration
- [x] BBS surface: telnet login shell + MAIN MENU — **full BBS landed**: welcome screen on login (shared `compose_welcome`: MOTD/unread/online-sample/featured/ticker; `theme_logo_ansi` verbatim to ANSI TTYPEs, art-flattened to plain glyphs otherwise — no ESC leaks, e2e-asserted; retro TTYPEs flip the wire to CP437 via `legacy-telnet::encoding` now using `rabbithole-art`'s real tables), boards shell (list → paged threads → paged posts → reply/new-thread, BOARD_POST-gated w/ the user's author seed), live lobby chat (`tokio::select!` on cancel-safe `read_line` vs bus; native↔telnet interop e2e), DM shell (threads/read/reply, read marks + receipts, auto-response via the shared path), `/go <keyword>` at every prompt (board:/area:/door: targets; bare-slug fallback; listing). Menu: B/C/D/F/M/O/Q (doors → O). Rate classes: legacy per command, post/msg on sends. 6 e2e tests. Deferred: live DM toast, kick/notice surfacing in chat, native-proto KeywordKind, CP437 multi-byte input editing
- [x] File browse + HTTP-link handoff — telnet `files` sub-shell (`ls`/`cd`/`get`/paging) with RBAC identical to Hotline's browse (FILE_LIST + per-folder SEE, drop-box contents hidden without DROPBOX_VIEW); `get` mirrors FILE_DOWNLOAD + drop-box rules and prints `<files_http_base>/files/<area>/<percent-encoded-path>` (link minting only — the HTTP file server is the web slice; empty base → polite refusal). Download counting happens on actual fetch (web slice)
- [x] Zmodem transfers over telnet — codec: `rabbithole-legacy-zmodem` (CRC16/32, ZDLE, hex/bin16/bin32 headers, data subpackets, ZFILE, sans-IO Sender/Receiver, fuzz-tolerant), now with offset resume on both sides (`Receiver::set_resume_offset` → ZRPOS(n) answer to ZFILE; Sender honors ZRPOS initial/mid-stream/post-ZEOF). **Wired into the telnet files sub-shell** (`apps/server/src/zmodem.rs`): `zget <name>` (RBAC identical to `get` incl. drop-box rule **plus the HTTP path's quarantine/hash-deny gates, now also on `get`**; download counted on completion) and `zput` (FILE_UPLOAD on the destination, no-clobber name vetting w/ basename stripping, declared-size/quota caps, finalize = blake3 → hash-deny → quota-on-actual → blob → `add_file` + `FileAdded`; interrupted uploads park in a TTL'd per-(account,area,folder,name) staging store and a re-offer resumes via ZRPOS(staged)). 8-bit clean over new `TelnetStream::read_binary`/`write_binary` (IAC doubled/undoubled, encoding bypassed); five-CAN cancel, idle timeouts, and an OO-aware residue drain return every exit to a usable prompt; `transfer` rate class on starts; audited. 8 e2e tests (0xFF-heavy both ways incl. deliberately-unescaped 0xFF through IAC doubling, ZRPOS download resume, reconnect upload resume, deny-at-finalize, cancel-to-prompt). Deferred: interop smoke vs real lrzsz/SyncTERM, ZSKIP/ZNAK recovery, persistent staging across restarts
- [~] Door games: DOOR32.SYS (+DOOR.SYS/DORINFO1.DEF) dropfiles — landed: `rabbithole-legacy-doors` (`DoorContext` + faithful writers/readers for all three formats, never-panic parsers) **plus the session-runner core**: `door` (`DoorDef` argv/dropfile/IoMode/NodeRange/daily-limit + `DoorRegistry` with TOML `[[doors]]` serde), `node` (`NodePool` + RAII `NodeLease`, lowest-free + range-scoped allocation, contention-tested), `session` (pure FSM Preparing→Running→Ended/TimedOut/Aborted, injected clocks, `prepare_dropfile` pinning the node), `bridge` (sans-IO CP437-clean pump w/ chunk-boundary-safe telnet IAC doubling + `BridgeStats`); 47 tests. **Runner wired into burrow**: `apps/server/src/doors.rs` (`DoorService` — boot-validated `[[doors]]` registry, shared NodePool, `%D/%F/%N/%H` token expansion + env, tokio process spawn, IAC-safe stdio pump, min(session-max, daily-limit) timeout→kill, RAII node release, `door-run`/`door-denied` audit) + a real telnet shell (`telnet.rs`: banner → AuthService login → menu with `doors`/`door <id>`), gated by `doors_enabled` (default off) + new `Caps::DOOR_RUN` (member+); 7 e2e tests. Deferred: DOOR32 comm-type-2 socket-handle inheritance, per-user daily accumulation, TOTP on telnet, TTYPE CP437 detection
- [x] Legacy security-level projection (RBAC → 0–255 SL + flags) for dropfiles — `server-core::security_level(&Subject)`: role bases Guest=10/User=30/Moderator=80/Admin=100/Superuser=255, twelve participation caps nudge ±2 within disjoint role bands (1–25/26–70/71–95/96–250) so cross-role monotonicity holds under any grant/revoke mask (tested adversarially); door dropfiles now carry the projection (hardcoded table deleted)
- [x] finger (79): empty = who list; user = profile+presence+.plan; /W; **forwarding refused**; output caps — `rabbithole-legacy-finger` (RFC 1288, pluggable `FingerDirectory`, control-char sanitized so a hostile .plan can't inject escapes, 32 KiB cap); per-persona opt-out + burrow wiring TBD
- [x] Legacy-surface class restrictions + per-listener toggles — `telnet_min_role`/`nntp_min_role`/`hotline_min_role`/`finger_min_role` (guest|user|moderator|admin, `member` alias, live-appliable, validated by `ctl config set`; default guest = prior behavior): telnet refuses pre-menu, Hotline names the requirement in the login error, NNTP answers 480 anonymous/481 authed-below-min, finger (anonymous protocol) refuses every query when min>guest; finger's accept loop moved into burrow with the per-IP conn budget for parity. 4 e2e + 8 unit tests
- [~] Wire the telnet/finger listeners into `burrow` — done: opt-in `telnet_enabled`/`finger_enabled` config, `burrow::legacy` adapters (`TelnetAuth`→`AuthService`, `FingerDirectory`→presence+`PersonasRepo`, invisible users hidden), listeners spawned at startup, e2e-tested. Art rendering in the telnet menus + full BBS navigation still to wire

## Wave 7 — Hotline compatibility
*Depends on: W2, W3, W4*

- [x] TRTP/HOTL handshake; 20-byte transaction codec; TLV fields w/ 16/32-bit size-dependent ints — `rabbithole-legacy-hotline` (handshake/transaction/field/reassembly/constants, big-endian, minimal-width int helpers, fragment reassembler with 16 MiB ceiling, fuzz-tolerant; 29 tests). Networking + login flow are later slices
- [x] Login (255−b obfuscation) + opt-in legacy credential; agreement/banner; Agreed/SetClientUserInfo flows; pipelined-early-request tolerance — `apps/server/src/hotline.rs` bridges the wire codec to the shared services: TRTP/HOTL handshake + OK, login (107) de-obfuscates creds and authenticates via `AuthService` (guest fallback when enabled), pushes agreement (109), dedicated non-cancellable reader + reassembler tolerate pipelined early requests; `hotline_enabled`/`hotline_addr` (5500) config, off by default; 3 e2e tests
- [x] User list + icon-ID mapping; NotifyChange/DeleteUser pushes; UserFlags — GetUserNameList (300) packs id/icon/flags/name from the presence snapshot (invisible users hidden); NotifyChange/DeleteUser (301/302) driven off the `EventBus`; SetClientUserInfo (304) re-publishes identity via `PresenceRegistry::rename`
- [x] Public chat, private chat rooms (112–120), IM (108) w/ quoting + auto-response — public chat (105→106) bridged to the shared `ChatService` lobby (Hotline/native/telnet share one room); private IM (108) routed via the per-server `Hub` (SERVER_MSG 104). **Private rooms landed**: a Hotline private chat IS a shared `ChatService` private room (Hub `chat_id↔room` projection): 112 create+invite (CHAT_CREATE_ROOM gate), 113 invite, 115 join (invite-only + bans enforced, returns subject + member records), 114 decline notice, 116/118 leave pushes, 119/120 subject; 105 w/ CHAT_ID routes room-scoped chat (members-only fan-out, lobby isolated); invites bridge natively both directions (`RoomInvited`), native `RoomKicked` → 118. **IM quoting (field 214 relayed) + away auto-response** (OPTIONS=4, shared once-per-away re-arm set; field 215 sets/clears away). 3 e2e tests. Notes: 117/119 pushes are Hotline-members-only; auto-response not durably stored (Hotline IMs ephemeral)
- [x] Threaded news transactions (370–411) mapped to boards; flat-news (101/102) projection — `apps/server/src/hotline.rs`: GetNewsCatNameList/GetNewsArtNameList/GetNewsArtData/PostNewsArt/DelNewsArt onto the shared `BoardService` (postable board→category, bundle→bundle; articles signed via the same author-seed as native; article ids a stable 32-bit projection of the event id); flat news GetMsgs/NewMessage/OldPostNews project the first postable board. Constants added to `legacy-hotline::constants` (verified vs Mobius)
- [x] File transactions (200–213); HTXF channel (port+1); FFO encode/decode (INFO/DATA forks, MWIN); fork-offset resume; folder lockstep — browse/info/download landed earlier; **upload (203) + fork-offset resume now complete**: `legacy-hotline::flatten` (FlatHeader/ForkHeader/InfoFork/`RFLT` FileResumeData — exact Mobius/HL1.9 bytes incl. the over-declared-fork-count quirk; 9 golden tests); upload runs the native UploadFinish discipline (blake3 → deny-hash → quota-on-actual → blob → node + FileAdded), 64 MiB cap, 60s idle timeout, MACR forks drained; download resume serves the DATA fork from the RFLT offset (envelope re-sent fresh); upload resume parks partial DATA bytes in the Hub (30-min TTL) and reports the parked size for the client to append; outcomes settle before FIN so tests are sleep-free. 7 e2e tests. Deferred: folder downloads/uploads (210/213) lockstep, banner, partial-upload persistence across restarts
- [x] Account admin transactions (348–355); access-mask projection (big-endian bit order, tested); kick/ban (110/111) — codec (`rabbithole-legacy-hotline::access`: AccessMask big-endian bit order, 38-variant Privilege, UserFlags verified vs Mobius/HL1.9, obfuscate/deobfuscate; 42 tests) **wired into burrow**: NewUser/DeleteUser/GetUser/SetUser onto the shared account service + RBAC (documented role↔mask projection: caps→bits outbound, nearest-role inbound with lossy cases noted; superuser never assignable/demotable), all admin ops capability-gated + audit-logged; DisconnectUser kicks via the native `ServerEvent::Kick` path (DisconnectMsg 111 delivered, cannot-be-disconnected respected) with 30-min in-memory login+IP temp-ban; UserBroadcast bridged both ways (native admin Broadcast reaches Hotline clients as ServerMsg 104); login reply carries the projected USER_ACCESS bitmap. 6 unit + 3 e2e tests. Deferred: ban persistence/unban op, hard delete, per-bit grant/revoke masks, login rename
- [x] `apps/tracker`: native registry + HTRK (5498) listing + UDP (5499) heartbeat registration — `looking-glass` daemon (TTL registry, HTRK UDP registration + TCP listing codecs with packet diagrams, native `LIST` status port; 18 tests). Signed descriptors / tracker gossip land with W9
- [ ] Compat rig: archived Hotline clients + mobius-driven integration tests

## Wave 8 — Web & desktop GUI
*Depends on: W2–W4 (W5 for transfer UI); parallel with W6/W7*

- [~] `ui-web` Leptos SPA — `rabbithole-ui-web` (Leptos 0.6 CSR: router + Login, Lobby chat + WhoList, **Boards** tree/thread views, **DMs** conversation+compose, **member Directory** with live search, nav bar, ThemeToggle, StatusBar; DOM-free `UiState`/`apply` reducer + `UiClient`/`MockClient` seam; **browser WebSocket transport** (`ws.rs`, web-sys, RHP-over-ws hello + command↔frame mapping + keepalive) behind a host-testable `wire`/`EventClient` seam; wasm-check in CI; 37 host tests). **Keyword bar landed** (PLAN §538, the SPA's first modal overlay): `ui-web::palette` — a pure, host-tested section catalog + fuzzy matcher (label-prefix > substring > alias rank; "members"→Directory, "music"→Radio) driving a `CommandPalette` component: ⌘K/Ctrl-K global toggle + a header affordance, `role="dialog"`/`aria-modal`, autofocused input, arrow/Enter selection, Escape + backdrop-click dismiss, navigating via the router. 6 palette + a11y-invariant host tests, and verified in the real browser (⌘K open, autofocus, "rad"→Radio filter, Enter→/radio, Escape close). **Toast notifications landed** (PLAN §9.3 humanized events): `ui-web::toasts` — a pure, host-tested `ToastQueue` (capped, stable ids, `ToastKind` Info/Success/Mail/Warn with glyph+class; 4 tests) + a `Toasts` `aria-live="polite"` region mounted at the app root, each toast dismissible + browser-auto-expiring; `AppState::notify`/`dismiss_toast`. Login raises a "Signed in as …" success toast + a "you've got mail — N conversations waiting" mail toast; verified in the real browser (both fire on connect, ✕ dismiss works). **Live transport wired (platform slice 1 of N)**: the `ws.rs` `WsClient` (which existed but was entirely unwired — the app only ever drove `MockClient`) is now bridged into `AppState` behind a `live` flag + a Login "Live connection" checkbox. `AppState::connect_live` (wasm) registers the transport's event sink (→ `UiState::apply`), conn sink (→ `set_conn`), and notice sink (→ radio/notice), then dials a real RHP-over-WebSocket session; the mock demo path is untouched. **Verified end-to-end against a running `burrow`**: the SPA connects to `ws://127.0.0.1:4674`, completes the Hello handshake, and the header goes Online showing the real server identity ("Connected to An Unnamed Burrow (0.88.0)") with the mock's amber theme reverting to the server's default. **Live auth + lobby chat landed (platform slice 2)**: `connect_live` now takes login/password and, once `Event::Connected` lands, sequences `AuthPassword` on the next microtask (`spawn_local` — can't dispatch inside the transport's borrow); the Login grows a password field (shown in live mode) and `AppState::send_chat` routes the lobby composer over the socket when live, else the mock. **Verified end-to-end against a real `burrow`**: created an account via ctl, signed in live as `alice`, sent a line, and it round-tripped back attributed to `alice` (server-assigned identity — a genuine round-trip, not a local echo). **Live who-list landed (platform slice 3)**: a host-tested `wire::frame_to_who` (decodes a `WhoList` reply → roster of screen names; the core `Event` enum has no roster variant, so it rides its own sink like FILE/notices) + a `WsClient` `who_sink`/`on_who`/`request_who`; `connect_live` registers the sink (→ `state.who`) and requests the roster right after auth. **Verified against a real `burrow` with two live sessions**: connected `alice` in one tab and `bob` in another — bob's PRESENT sidebar shows *both* users, fetched live. **Live presence deltas landed (platform slice 4)**: `wire::frame_to_presence` decodes `UserJoined`/`UserLeft` pushes → a `PresenceDelta` (host-tested) + a `WsClient` `presence_sink`/`on_presence`; `connect_live` folds deltas into `state.who` (add-if-absent / retain). **Verified against a real `burrow` with two sessions**: with `alice` connected, `bob` joining flipped alice's sidebar `alice`→`alice,bob` live (no re-request), and closing bob's tab flipped it back to `alice` — both directions over the socket. **Live board list landed (platform slice 5)**: host-tested `wire::board_list_request` + `frame_to_boards` (decodes a `BoardList` reply → `state::Board` rows) + a `WsClient` `board_sink`/`on_boards`/`request_boards`; `load_boards` is now live-aware (requests over the socket when live, else the mock pull) so the existing Boards-view-on-mount call just works. Added a ctl `board-create <slug> <title> [description]` (kind-2 board) to seed + a genuine operator tool. **Verified against a real `burrow`**: `board-create`d two boards, connected live, and the Boards view rendered them ("General Chat — Anything goes", "File Talk") fetched over the socket. **Live threads + posts landed (platform slice 6)**: refactored `state::Thread`/`Post` ids `u64`→`String` (hex blake3 live, `t<n>`/`p<n>` in the mock) across state/mock/`BoardView`; host-tested `wire::{thread_list_request, frame_to_threads, thread_request, frame_to_posts}` + `id_to_hex`/`hex_to_id`; `WsClient` `thread_sink`/`post_sink` + `request_threads(board)`/`request_posts(root)`; `select_board`/`open_thread` are live-aware (request over the socket, reply folds via sink) so the existing BoardView clicks just work. Added ctl `board-post <board> <author> <subject> <body>` (reuses `handlers6::author_seed`) to seed. **Verified against a real `burrow`**: `board-post`ed three threads, connected live, and the BoardView rendered the real thread list ("Best ANSI art?", "Introduce yourself", "Warren rules" by alice) and, on opening one, its live post ("Share your favourite pieces."). **Live board posting landed (platform slice 7)** — boards are now fully interactive live: `wire::post_create` (new-thread `PostCreate`), `WsClient::send_post`, and `AppState::post_thread` (live: send then re-request threads — the ordered socket includes the committed post; mock: local prepend). BoardView grew a "New thread" compose form (subject + body). Added ctl `board-post` earlier for seeding. **Verified against a real `burrow`**: on an empty live board, composed "Hello from the live web client" → it appeared in the thread list; opening it loaded its post ("Posted live over WebSocket.") authored by `alice@an-unnamed-burrow` (the server-assigned identity — a real server-side post, not a local append). **Adversarial review pass + hardening (0.96)**: a 27-agent find→verify workflow over the live-transport slices raised 22, confirmed 19. Fixed the real ones: **[HIGH]** the DMs/Directory/Files/Radio mock loaders had no `live` branch, so a live session rendered seeded MockClient data as real — added `skip_mock_load()` gating (verified: live directory now shows 0 members, radio off-air, vs. mock before); **[med]** keepalive loop leaked a zombie ping task per reconnect (keyed on the shared `alive` flag) — added a per-socket `generation`; **[med]** live login with a blank handle connected but never authed — now requires a handle; **[med]** `frame_to_boards` surfaced categories/bundles as selectable boards — filtered to `kind==2`; **[med]** requests during the CONNECTING handshake threw spurious `CommandFailed` — `write` now skips silently until OPEN; **[low]** `hex_to_id` could panic on a 64-*byte* multibyte string — added `is_ascii` guard; **[low]** ctl `board-post` author now matches the network `screen_name@origin` format. Deprioritized (documented): stale-reply overwrites are mitigated by the single ordered socket; compose-clear-on-failure + presence-session-id keying are minor. **Live reply threading landed (platform slice 8)** — boards are now fully complete live (browse → read → post thread → **reply**): `wire::post_reply` (a `PostCreate` with `parent = Some(root)`, no subject), `WsClient::send_reply`, and `AppState::post_reply` (live: send with the open thread's root id, then re-request its posts; mock: local append). BoardView's reader panel grew a reply composer (shown when a thread is open). **Verified against a real `burrow`**: opened a live thread (1 post), replied "Great question! I love the classic warez ANSI packs." → it appeared as a 2nd post, both authored by `alice@an-unnamed-burrow` (server-side). **Live DMs landed (platform slice 9)** — full two-way direct messages over the plaintext DM path (E2EE opt-in stays off; `encrypted: None`): host-tested `wire::{dm_threads_request, frame_to_dm_threads, dm_history_request, frame_to_dm_history (id-sorted), dm_send, frame_to_dm_received}`; `WsClient` gains three DM sinks + `request_dm_threads`/`request_dm_history`/`send_dm`; `load_dms`/`select_dm`/`send_dm` are live-aware, and `on_dm_received` folds a live push into the matching (or a fresh) conversation. `select_dm` upserts the peer thread + a "Message a handle…" starter lets you begin a fresh conversation live. **Verified against a real `burrow` with two users**: alice started a DM to bob and sent → it appeared in bob's inbox as the "alice" conversation with her message; bob replied → **alice's open thread updated in real time via the receive push** (no re-select). **Live files landed (platform slice 10)** — the FILE-family transport (`file_command_to_frame`/`frame_to_file_events`/`WsClient::dispatch_file`/`on_file_event`) already existed but was unwired into the live path: now `AppState::dispatch_file` routes over the socket when live (else the mock seam) and `connect_live` registers the file sink (folds `FileEvent`s into `FilesState`), so `load_areas`/`open_area`/`refresh_files`/upload/download all go live. Added ctl `file-area-create <slug> <title> [description]`. **Verified against a real `burrow`**: `file-area-create`d two areas, connected live, and the Files view rendered them ("Warez — Fresh releases", "ANSI Art"); opening one fetched its (empty) folder listing live. **Live file upload + download landed (platform slice 11, scoped by an agent subteam)**: upload was already live (bytes→blob→node→listing); the gap was inline **download discarding the bytes** — `wire::frame_to_file_content` now decodes `FileContent`→`DownloadedFile` and `WsClient::save_download` delivers it to the browser as a real file (Blob + object-URL + a detached `<a download>` click; web-sys `Blob`/`BlobPropertyBag`/`Url`/`HtmlAnchorElement` features added). **Verified against a real `burrow`**: uploaded `note.txt` (25 B) live via "Upload sample", then Downloaded it — the transfer row hit "DONE 100%" with no console errors and the server-side **Downloads counter bumped to 1** (proof of a real round-trip, not a no-op). **All primary SPA views are now fully live off the mock: lobby chat, presence, boards (browse/read/post/reply), DMs (list/history/send/receive), and files (areas/folders/upload/download).** **Live directory + profile card landed (platform slice 12)**: host-tested `wire::{directory_search_request, frame_to_members, profile_get_request, frame_to_profile}`; `WsClient` gains members + profile sinks + `request_directory`/`request_profile`; `load_members` is live-aware (empty-query DirectorySearch → all visible members, `online` filled from the live roster), and `AppState::select_member` fetches the `ProfileCard` live. New `state::MemberProfile` + a richer profile card (quote/location/interests/plan/pronouns/pronouns + online-from-`online_transport`). Added ctl `persona-set <screen_name> <quote> [location]` to seed profile text. **Also fixed drifted PWA icons** — `crates/ui-web/assets/icon-{192,512}.png` had a trunk-re-encoded (bloated 26 KB) version committed at 0.99.0; regenerated from `icon_rgba` (the 4.7 KB source-of-truth) so `pwa::tests::checked_in_icons_match_the_generator` passes again. **Verified against a real `burrow`**: seeded alice/bob/carol + persona-set bios; the live directory listed the real members (not the mock trio), search filtered client-side, and clicking a member loaded their live profile card (alice **Online** + "Curious about everything." + Location Wonderland; bob Offline + his bio). **Avatar image landed (platform slice 14)** — completes the profile card: `frame_to_profile` now captures the avatar's hex id; a host-tested `wire::{blob_get_request, frame_to_blob, blob_to_data_url}` (a tiny dependency-free base64 encoder + PNG/JPEG/GIF/WebP magic-byte MIME sniff, RFC-4648-vector tested) turns the fetched `BlobData` into a `data:` image URL. `WsClient` gains an avatar sink + `request_blob`; the `on_profile` sink defers (`spawn_local`) a `BlobGet` when the card has an avatar, and `on_avatar` folds the data URL onto the selected profile. The card renders a circular `<img>`. Extended ctl `persona-set` with an optional `avatar_path` (reads a file → `blobs.put` → sets the persona's avatar id). **Verified against a real `burrow`**: seeded alice's avatar from the PWA icon PNG; her live profile card rendered it as a `data:image/png;base64,…` `<img>` the browser decoded (`naturalWidth` 192). **Reconnect-UX landed (platform slice 13, the last subteam-ranked track)**: pure host-tested `conn::conn_banner(conn, live) -> Option<ConnBanner>` (only live+unhealthy states banner; mock never does); `WsClient::redial` (reset backoff + open now, guarded by a new `ws.is_some()` duplicate-socket check in `connect`); `AppState::reconnect`/`online`; the `on_conn` sink toasts the drop edge exactly once (Online→Reconnecting guard); the StatusBar renders the banner with a Retry/Reconnect button; the Lobby + DMs composers disable while `!online()`. **Verified against a real `burrow`**: connected live (no banner), killed the burrow → header "Reconnecting…", the blue banner + amber drop toast + a greyed-out disabled Send button all appeared; restarted the burrow + clicked "Retry now" → back to Online, banner gone, composer re-enabled. **The web SPA platform effort is complete: every primary view is live off the mock, plus binary transfer, directory/profile, and connection-lifecycle UX.** **Second adversarial review + hardening (platform slice 15)**: a 15-agent find→verify workflow over the newer slices (0.96–0.103) raised 10, confirmed 9 (several the same root cause). Fixed: **[MED] avatar attaching to the wrong member** — an id-less `BlobData` reply was applied to whatever profile was selected, so a late avatar could paint onto an avatar-less member persistently; now a FIFO pending-hex queue in `WsClient` pairs each reply and `set_avatar_src` only attaches when the hex matches `selected_profile.avatar_hex` (unit-tested + live-verified: alice's avatar renders, never leaks to no-avatar bob); **[MED] error-reply double-fire** — `frame_to_file_events` surfaced *any* family's error into the file panel; now gated on `Family::FILE` (errors preserve their request's family); **[MED] board composers** (new-thread + reply) now disable on `!online()` like lobby/DMs; **[low] directory online badges** recomputed from the live roster at render (`matching_members`) so presence deltas keep them fresh (and they're correct even if the directory reply precedes the roster); **[low] DM-history** got the same FIFO-peer guard (drops a stale reply that would briefly show another peer's private messages); **[low] persona-set** avatar read moved to `tokio::fs`. 228 host tests + wasm + burrow green. NB: `trunk build`/`trunk serve` re-encodes `crates/ui-web/assets/icon-{192,512}.png` (pixel drift fails the pwa icon test) — `git checkout` them before committing, same as `.gitignore`. **Router remount fixed (0.193.0)**: the `<Routes>` block was wrapped in a `move ||` child keyed on the focused burrow, and that dynamic child re-ran on *every* navigation (leptos_router warned "only render `<Routes/>` once" per click, even with nothing tracked) — rebuilding the whole routed tree twice per click and panicking any effect the first build had queued (`OwnerDisposed`, the thread-open/art-gallery crashes). Now a keyed `<For>` on `focused_id`: rebuilt only on a burrow switch. **Live admins get the console**: `Event::Authenticated` carries `role` + `caps` off `AuthOk` (additive on the `#[non_exhaustive]` core enum) and the live sink sets `is_admin` from `state::role_is_operator(role)` — before, only the demo's seeded `rabbit` handle ever saw an Admin entry. Also: reply → thread list refreshed (reply counts agree with the reader); upload reads its area untracked. **Connect screen + placeholder (0.194.0)**: the default burrow address is `ws://localhost:4654` (the real `ws_addr` default; it said 9000, the demo's pretend port); the demo toggle only renders under the `demo` feature and live is the only path otherwise; field hints, autocomplete, a tagline. The app's placeholder session (`ServerId::local`, `is_placeholder()`) no longer renders as a "Demo — Offline" rail tile, and a `PlaceGuard` sends burrow routes (`palette::needs_a_burrow`) to `/` while only the placeholder is focused — closes the hole where a shipped build could show `MockClient::new()`'s seeded boards/DMs/files as a real place via a typed URL or after leaving the last burrow. `/settings` is warren scope (it wore a burrow sidebar). The sidebar `Nav` reads the focused session reactively (`focused_tracked`) instead of capturing one session's signals at mount — after Leave + rejoin, pips and the Admin entry were still the dead session's. **Phone layout (0.195.0)**: below 720px the burrow rail is gone (it was a quarter of a 390px screen) and a "Warren" tab at the head of the bottom bar opens a `WarrenSheet` (burrows to switch/add, People/Transfers/You/Settings, Leave); the bottom bar now shows in warren scope too (`SideNav` marks `warren-scope` as a class and the stylesheet decides per width) and `Nav` lists `sections_for(scope_of(path))`; tabs share the width (`flex:1 1 0`) instead of overflowing off-screen with the scope label eating the first slot; the header is one row (Leave lives in the sheet); the format bar scrolls instead of wrapping "Markdown" onto its own line (also in the narrow new-thread column on desktop). **Polish (0.196.0)**: DM conversation rows show the peer's warren mark (not a gradient blob), a last-message preview + time (`DmThread::preview`, host-tested: fetched history wins over the list summary; `last_text`/`last_at_unix_ms` were on the wire and dropped), and an unread pip; DM message bursts carry the sender's mark like the lobby; the Directory's presence dot reads the live roster (it read the stored member row's `online`, which no presence delta ever updated, so everyone showed grey); the desktop header orders presence + Leave with the icon cluster on the right (unordered, they rendered before the burrow's name). **Admin console asks for real keys (0.197.0)**: `load_config` requested the demo mock's dotted spellings (`server.name`, `chat.slowmode_secs`), which a live burrow answers NotFound, so an operator saw nothing but the gateway toggles; it now loads `admin::OPERATOR_KEYS` (name/motd/agreement/registration_mode/guest_enabled, chat + transfer limits, ws_public_url/advertise_host/announce_*) under the server's own names, the mock seeds the same keys (host test: every operator key answers), and a Save was verified live (`Config saved and applied live` → `ctl config-get` agrees). Config rows are a key/value/Save grid. Also verified this session against real burrows: chat, boards + replies, DMs both ways, upload/download (52 B and 300 KB), broadcast notice, agreement sheet, two burrows at once with People merged and switching, the release bundle served by `burrow --http` with the service worker controlling. README status bumped. **Design polish pass, Operate mode (0.198.0, via the `impeccable` skill's polish playbook)**: every glyph that stood in for an icon is drawn now (`icons::{chevron_left,upload,arrow_down,arrow_up,link,close,key}_icon`, host-tested; empty states take a section route and draw that section's icon in a soft disc instead of a dingbat); the tiny uppercase tracked eyebrows that served as headings became real headings (`.rh-panel-title` at font-lg + `.rh-panel-sub`, sentence-case sidebar scope, header status, settings/you group heads, login labels, table heads, news eyebrow); lists are hairline rows, not same-size cards (boards + file areas with the section icon and unread pips, members with their warren mark, Looking Glass rows with uptime pill + listeners + endpoint + a secondary Connect); posts are hairline blocks headed by the author's mark and `created_at_unix_ms` (on the wire, dropped before; `state::Post.at_unix_ms`); the admin's per-row Save is secondary; the caret is themed. The detector's nine pre-existing findings were fixed (three 3px accent side-borders on the welcome sheet, toasts and blockquotes; four overshoot easings; the transfer bar now animates `transform:scaleX` instead of width) and it reports zero. `.rh-dot{order}` is header-scoped (it floated a list row's dot to the far end). **Signed-out recovery (0.199.0, the critique's P0 via `/impeccable harden`)**: a refused password or a dead resume token used to leave the burrow "Online" with skeletons forever, one toast per retry, and no sign-in anywhere but Leave. Any pre-auth `CommandFailed` now signs the burrow out once (`AppState::sign_out`: deliberate Disconnect so the transport stops retrying, session + tile dropped via the shared `drop_session`, saved handle kept, `pending_endpoint` + a new `pending_notice` set, stale toasts cleared) and the route guard lands on the connect form, which shows the reason above the fields ("Your session on Wonderland expired. Sign in again." / "Wonderland didn't accept that handle and password."). `ToastQueue::push` collapses an identical repeat (host test). A list request that fails mid-skeleton clears `loading` and sets `UiState::load_failed`; Boards, DMs and Directory show `LoadFailed` ("Couldn't load the boards. Try again") instead of shimmering or claiming emptiness (host test). An empty password now really is a guest visit: `command_to_frame` maps it to `AuthGuest` (host test; verified live as "visitor (guest)"). Critique snapshot: `.impeccable/critique/2026-09-18T03-32-44Z__crates-ui-web-src-components-rs.md`, 21/40, dual-agent. **Phone shell rebuilt (0.200.0, critique P1 via `/impeccable adapt`)**: below 720px the bottom bar is now `PhoneTabBar` with five thumb-reach tabs (Warren → the switcher sheet, wearing the unread-elsewhere badge; the focused burrow's tile + name, or "Burrows" → Looking Glass when none; People; Transfers; You) at 11.2px labels, and the burrow's sections moved into `SectionStrip`, a scrolling chip strip under the header (burrow scope only, with pips and Admin), so eight sections no longer fought for a 390px bar at 9.92px (the detector's `undersized-ui-text` ×14) and the bar no longer changed shape between layers. The sidebar is hidden on phones; toasts are bottom snackbars above the bar; an empty Present strip hides itself (`:has(> ul:empty)`); Looking Glass rows stack with a full-width Connect; the empty-state copy no longer points "left"/"below" on a screen with neither. Desktop is unchanged (tab bar + strip `display:none` wide). `section_unread` is shared by the sidebar and the strip. **Composer distilled (0.201.0, critique P1 via `/impeccable distill`)**: `Composer` has a `chat` variant (lobby, DMs): one row that grows with line breaks (`compose::rows_for`, host-tested, capped at six), an "Aa" toggle that reveals the formatting bar on demand, a drawn send button (`icons::send_icon`), Enter still sends, no hint line; posts and replies keep the tall editor with preview and hint. The board's new-thread form is folded behind a "New thread" button beside the board title (`.rh-panel-head`) and closes after posting. The dead single-line `.rh-compose` rules are gone. **Title bar quieted (0.202.0, critique P1 via `/impeccable quieter`)**: the connection label only speaks when there is something to say (Connecting…/Reconnecting…/Offline; healthy is the dot alone, so "Online" no longer sits beside a presence menu that also says Online); the chimes/appearance/mode icon cluster left the header for an Appearance group in Settings (segmented theme pack + light/dark/match-system, `AppState::set_pack`/`set_mode`), and Settings got a rail tile at the foot (it had no desktop entry point but ⌘K); Leave shows only in burrow scope, asks first (`app::confirm_leave`, native confirm in the shell), and keeps the handle in the recent list (it forgot the whole entry, costing the handle on every Leave). The `ThemeToggle` component and its rules are gone. **The ember split lands (0.203.0, critique P1 via `/impeccable colorize` + `typeset`)**: the design spec's §8 tokens exist now: `--rh-brand` (Clean: #E86A1F light / #FF8A3D dark), `--rh-surface-2` (Clean: #eef1f6 / #242833) and `--rh-font-display` (Space Grotesk, self-hosted latin woff2 under OFL in `assets/`, shipped by `index.html` and served as `font/woff2` by `burrow --http`); Retro and High Contrast collapse brand into accent and surface-2 into surface. The rail sits on surface-2 with the unified tiles and the active tile's edge bar in ember; the sidebar is a neutral step off it; transfers (bar, pills, arrows) and the You mark's ring are ember; the phone bar's warren tabs light in ember while the burrow tab keeps the burrow's accent. The wordmark, burrow names, view titles, Looking Glass names and letter tiles are set in the display face at -0.02em; body stays on the system face. The rail dot's infinite pulse is gone (the spec asks for stillness). Every pack × mode still defines every variable the stylesheet references (host test). **Polish close (0.204.0, `/impeccable polish`)**: the detector's rendered-page findings are fixed — the admin config list scrolls inside a bounded height so the console's first screen is both columns, a panel title that follows content gets more space above than below (`.rh-panel-title:not(:first-child)`), hints, Looking Glass descriptions and settings notes are capped at 70ch; the three stray empty-state idioms (Radio "(off the air)", Admin "You do not have admin access.", italic list notes) are on the one `EmptyState` voice; a board opened by link asks for the board list so its title is its name, not its slug. Source detector: 0 findings (Space Grotesk waived at its declaration: the design spec pins it). The critique snapshot is closed and kept under `.impeccable/critique/` (the live-server scratch is ignored). Guest DMs (0.205.0): a guest opening DMs used to see "Couldn’t load your conversations. Try again" — the server refuses a guest’s conversation list with Forbidden, so the retry could never work. The session now knows it is a guest (`Session.is_guest` from `AuthOk.role`), `load_dms` never asks for one, and the DM section shows one `EmptyState` ("Messages need an account") whose Sign in brings this burrow’s connect form back, prefilled, with a line saying why. Verified live against a local burrow at 1280×800 and 390×844; detector 0 findings. Connect window (0.206.0): the web-style sign-in card is now a Mac welcome window. Left pane: the app logo (a 256px cut of the desktop icon art, shipped as `assets/logo.png`), the wordmark, the connect form and the version; right pane: the Looking Glass as Hotline’s tracker window (design spec §3) — *Your burrows* over *Discover*, one dense 32px list with a pixel mark per burrow, mono numerals, a filter, refresh, and a status strip naming the source. One click or arrowing selects (fills the form and names the button: “Connect to neo@zion”), double-click or Return connects; the selected row opens to show the full blurb, address and listeners. Pure rules live in `connect.rs` and are host-tested: the built-in sample is never offered as a place, a saved burrow is never listed twice and wears its directory name, the census counts only what a source reported. The demo checkbox and chips are gone (a `demo://` address is the whole question). In the desktop shell both panes run to the window’s top edge under the traffic lights. New `--rh-on-brand` ink token with an AA contrast test in every pack. Fixed on the way: a guest’s remembered handle kept the burrow’s ` (guest)` suffix and grew another each visit; a failed refresh relabelled a good listing as the built-in sample (it now keeps the last one); an empty handle made Connect do nothing silently; connecting from the list panicked (“could not get stored value”) because the screen is disposed mid-connect. Verified in the headless browser at 1100×760 light and dark and 390×844, and in the native window; detector 0 findings. Header, news and names (0.207.0, from Kevin’s 2026-09-18 list, tracked in `docs/design/2026-09-requests.md`): the burrow news is a flush band at the top of the chat column instead of a rounded card floating in it; the header mark is no longer clipped by the title’s own overflow; the status control is a pop-up button with a listbox, so each status wears its colour (the `<select>` could not, and its label sat low), with arrows, Return, Escape and click-away; **Leave works in the desktop app** — it used `window.confirm()`, which a webview answers `false` without showing anything, and now asks through the app’s own `alertdialog` (`AppState::confirm`, an intent the app carries out, not a stored closure) and is red; names in the lobby and the Present list open the person. Bookmarks (0.208.0): the burrow browser keeps a **Bookmarks** shelf above Recent and Discover (`bookmarks.rs`, `rh.bookmarks.v1`: an address and your name for it, never a credential). Bookmark any listed or recent burrow from its opened row, add one by address with the + in the browser’s header, rename, remove; Recent rows gain Forget. A bookmark outlives its listing and keeps your name for it; a place sits on one shelf only. Status: the directory’s word when it lists the place, otherwise the client’s own knock (`probe.rs`: open the same WebSocket a connection would, send nothing, close; through the same endpoint rule as a connection, five-second budget), and “unknown” while a knock is out, never “down” by default. The opened row’s detail is a sibling of the row button, so its controls are real controls. The in-app Looking Glass (`/servers`) is now the same `BurrowBrowser`, standing alone with a Connect on the opened row; the old card list and its styles are gone. Address comparison ignores the scheme’s case (two new tests caught `WSS://` not matching `wss://`).
- [~] Files UI: browse, upload/download (WS + fetch), transfer queue — `ui-web`: `files.rs` (`FilesState` reducer + `Transfer` queue model), FILE-family `FileCommand`/`FileEvent` + `file_command_to_frame`/`frame_to_file_events` in `wire.rs` (host-tested), FolderBrowser/FileDetail/TransferQueue components + `MockClient::dispatch_file`. Live async transfer progress + real download bytes land when `WsClient` grows FILE-family dispatch Demo downloads that land (0.209.0): a demo download used to “succeed” with a buffer of zeros that nothing saved, and in the desktop shell it went to the swarm asking for the all-zero root and failed. Seeded files now have real contents (`demo_files.rs`: a readable readme, a CP437 ANSI screen, and a genuine one-member `-lh0-` LHA archive with a tested CRC-16/ARC), listed sizes are the length of those bytes, demo sessions never go to the swarm, and both the socket path and the demo path save through one door (`save.rs`): Blob + `<a download>` in a browser tab, and in the desktop shell a new `save_file` command that writes into the downloads folder under a sanitised name, never overwrites (Finder-style `name (2).ext`), and reports the path in a toast. The deliberately failing fixture still fails. Where a download goes (0.211.0, desktop): it **asks where to save** in a native save panel unless a download folder is set; with one set it goes there without asking, optionally into **a folder per burrow**, and never over an existing file. The shell owns the preference (`apps/desktop/src/downloads.rs`, `downloads.json` in the app config dir) and resolves every destination itself, before a byte moves: a folder is only ever chosen in a native folder panel and a one-off path only in a native save panel, so the webview never names a path, and what it does supply (a file name, a burrow’s name) is reduced to one safe path component (`sanitize_component`: separators, colons, control characters, dot-runs, 64-char cap). Swarm downloads keep a stable name so `.rhstate` resume still works. `tauri-plugin-dialog`, used from Rust only (no dialog capability granted to the webview). Settings → Downloads shows what a download will do, Choose/Change folder, Ask each time, and the per-burrow switch; in a browser tab it says the browser decides. **A download no longer needs someone to be seeding (0.212.0, desktop):** the shell sent every hashed file to the swarm and treated “no peer has it” as a failure, so on a burrow where nobody happens to be sharing (most of them, most of the time) downloading in the app did not work. `swarm::run_download` now routes (`choose_route`, pure + tested): peers when anyone has it, else the burrow itself through the ticketed, resumable, blake3-verified `Client::transfer_download`, reporting progress in the same per-unit events so the Transfers row cannot tell the difference; a stale swarm partial is cleared first because the origin resumes by length. The file detail gains **Get it from**: Best available / Peers only / This burrow only (`settings::DownloadFrom`, remembered), and a peers-only failure says so in words. Proven against a real burrow in `apps/desktop/tests/origin_fallback.rs`. Sources on *other* burrows stay out: a ticket is honoured only inside the burrow that signed it (see `docs/design/native-swarm-backend.md`). **The app can give back (0.213.0, desktop, opt-in):** Settings → Downloads → “Help other people’s downloads”. When on, a file downloaded from a burrow is offered to other people on *that same burrow*, from this machine, while the app is open (`apps/desktop/src/seeding.rs`, mirroring `rabbit swarm share`: a per-burrow `SeedStore` and `PeerServer` bound to that burrow’s key, `swarm_contact` + `swarm_advertise`, re-announced at two thirds of the granted TTL, re-announced on reconnect because adverts die with a session). The burrow still decides who may fetch: a fetcher must present a capability it signed. Off means off now: adverts withdrawn, endpoints closed. Sharing never fails a download; a refusal is said in Settings. Proven against a real burrow in `apps/desktop/tests/seeding.rs`: Alice’s download comes from the burrow, she shares it, Bob’s comes from Alice verified, she stops, the next one goes back to the burrow. Not yet: seeds are in memory only (a restart forgets them), and an inline socket download is not seeded.
- [x] Art rendering (canvas) — `ui-web::art`: pure `parse_art`/`to_draw_ops` (reusing `rabbithole-art`'s Canvas/Cell/PALETTE, no parser duplication) + wasm-gated `paint` to an HTML canvas; ArtCanvas/ArtGallery components; 7 host tests
- [~] Design tokens; **light/dark mode** (OS-follow + manual override) across all rich clients — `ui-web::theme_css`: design tokens as CSS custom properties, `ThemeChoice` + pure `effective_mode(os_pref, override)` resolution (host-tested), wasm-gated localStorage persistence + reworked ThemeToggle. Retro/HighContrast pack plumbing (`root_style(pack, mode)`) is in place. **RH-39 TUI appearance**: Ctrl-R cycles Clean/Retro/High Contrast, Ctrl-T cycles Auto/Light/Dark, and preferences persist atomically in the platform config directory (or `--preferences FILE`). Auto follows a supported startup `COLORFGBG` hint with a Dark fallback; manual choices win. Read/save failures recover without terminating the client; High Contrast preserves its palette and normal packs retain readable server accents. Desktop parity remains a later slice. **Modern-design pass landed (W8 polish)**: the SPA stylesheet was rewritten for depth and craft — a glassy sticky command bar with a CSS-drawn concentric "burrow-hole" brand mark, `color-mix`-derived hairline borders/hovers/tints that flow from the six themeable colour tokens (so light/dark and server accents still cascade), elevated cards + focus rings + hover motion, avatar presence chips, pill nav/compose, and a `max-width:720px` responsive stack (columns collapse, the command bar wraps). The token set gained an **elevation ramp** (`--rh-shadow-1/2/3`), a fuller **type scale** (`--rh-font-lg/xl/2xl/3xl`), a **radius scale** (`--rh-radius-sm/xl/full`), and `--rh-space-5/8`, parameterised per pack via `SharedSpec` (Clean elevated, Retro/HighContrast flat). Palettes unchanged (so the PWA icon/manifest + TUI are untouched); verified in the real trunk-built wasm app (login, lobby light+dark, boards reader, files, directory) with 201 host tests green
- [x] Theme packs: Clean (default), Retro (CP437/scanlines/ANSI palette), High Contrast; shareable token files — `ui-web::packs`: `PackTokens` (complete `--rh-*` token set per pack×mode; Retro = core Retro palette + scanline gradients + monospace/boxy; High Contrast = AAA palettes w/ WCAG contrast-floor tests), `to/from_tokens_json` (tamper-tolerant: unknown keys ignored, missing vars defaulted) as the server-theme-bundle seam; `ThemeChoice` now pack×mode (legacy persistence parsed), 3×3 picker in ThemeToggle; 120 host tests + wasm check green. Runtime pack-JSON loading from a server bundle is the follow-on
- [~] Server theme bundle application (accents, icons, art, sounds) w/ safety rails — server-side signed bundles, token/contrast validation, persistence, admin publish/clear, and per-account `ThemePrefGet/Set/State` are implemented. **RH-40 live delivery**: authenticated web/desktop sessions fetch `ThemeGet`, verify against the handshake key, independently enforce the CSS token grammar, and cache by content hash. `ThemeChanged` refreshes already connected sessions after publish/clear or account preference changes; missing/rejected bundles clear the overlay. Request IDs and connection generations isolate replies by burrow and survive reconnects. Account opt-out returns `NotFound`; High Contrast preserves the local palette and geometry. Editor previews still take precedence. Asset rendering, sound sets, and richer per-burrow controls remain RH-42/RH-43/RH-44.
- [~] Theme editor panel in web admin (upload assets, accents, live light/dark/retro preview) — `ui-web::theme_editor`: pure `EditorState` reducer (SetColor/SetShared/LoadJson/Reset/SelectBase; hex + CSS-length validation as state errors, dirty tracking, `export_json`), WCAG contrast checker promoted from packs' test code (`contrast_warnings` <4.5:1 warn-only); `ThemeEditorPanel` in the admin console (base-pack selector, light/dark tabs, swatch rows, scoped live-preview pane, Apply/Revert session via a `custom_pack` override slot in `resolve_root_style`, export textarea + tolerant JSON import); publish-to-server stubbed pending the bundle-application slice. 16 new tests (136 crate total) + wasm check green. Remaining: asset uploads, server bundle publish, cross-session persistence
- [~] Embedded web client served by server; installable PWA — `apps/server/src/http.rs`: hand-rolled HTTP/1.1 GET/HEAD listener behind `http_enabled` (default off; `http_addr` :8080, `http_web_root`): serves the SPA static root (index.html at `/` and supported client routes such as `/lobby`, `/files`, and `/boards/:slug`; real assets win, missing assets stay 404, symlinks canonicalized under root, no listings) + a generated `/manifest.webmanifest` (server name, standalone, `theme_accent`) when the root ships none — `trunk build` output is the contract; `/files/<area>/<path>` serves the telnet `get` handoff with bare-guest SEE|FILE_LIST checks, drop-box/quarantine/deny-hash all one 404, download counted at the byte-serving hop; strict per-segment percent-decode sanitization (`%2F` can't mint segments), 8 KiB head cap, conn+legacy rate classes, FIN+drain close. HTTP unit and end-to-end coverage, plus a real-browser direct-link/sign-in/reload regression with service workers blocked (RH-153). CLI enablement: `burrow --http [--http-addr ADDR] [--web-root DIR] run` overrides the config at startup (each flag implies --http; README quickstart updated). **PWA landed**: `ui-web/assets/sw.js` (versioned app-shell cache, fetch-then-cache for trunk's hashed bundles, network-first navigations w/ cached-shell offline + deep-link fallback, hard `/files/` bypass, old-cache cleanup), checked-in `manifest.webmanifest` (wins over the server's generated one) + reproducible maskable icons (pure `icon_rgba` generator, drift-tested against the checked-in PNGs), wasm-gated registration (feature-detected, never fatal), trunk `index.html` + `main.rs` bin wiring (9 tests; trunk absent in CI env — wiring pin-tested). **The burrow never serves its own folders (0.267.0)**: `http_web_root` is an ordinary config key that `CONFIG_ADMIN` sets live, and the static route serves any regular file under it to anonymous GETs — so pointing it at the data directory answered `GET /identity/server_ed25519.seed` for anybody at all, with no session and nothing in the audit log (`ConfigSet backup_dir=<web root>` + `BackupCreate` was the same hole through the snapshot folder). Verified against a throwaway burrow: it returned the 64-byte seed and the whole database. Three layers now: `http::private_dirs` refuses the data directory and the snapshot folder on the canonicalized path whatever the web root says (so a symlink into them is refused too, and it holds when no validation ran — a hand-edited config, a `--web-root` flag); `set_key` refuses an `http_web_root` that contains, equals or sits inside either, and a `backup_dir` inside the web root, with both sides made absolute so `target/x` and `/here/target/x` are one place; and `data_dir` is refused over the wire, since the console was only *told* it was read-only and moving it would have pointed every other check at an empty folder. A snapshot is also kept to its owner now (`0700` on the folder, `0600` on `burrow.db`): `VACUUM INTO` wrote at the umask, so every console-made snapshot held a world-readable copy of every password hash. Found by the adversarial review of docs/design/snapshot-download.md, which was written to argue that a snapshot download would be the first way to serve the seed — attack the premise first. Real Trunk output has been exercised against an isolated burrow in Chromium (RH-40/RH-153). Bounded HTTP/1.1 keep-alive now reuses connections with strict request framing, sequential pipelines, idle/head-byte/request-count limits, and unchanged rate classes (RH-46); a browser regression covers initial load and immediate reload under production-default limits. Single-byte-range GETs now resume static files and guest-visible downloads with bounded parsing, 206/416 metadata, HEAD ignoring Range, and unchanged access checks (RH-47; see `docs/deployment.md`). **RH-45 PWA feedback**: a compact update notice offers an explicit reload, including builds whose worker script is unchanged; other tabs keep their drafts. Saved-shell provenance and app-host reachability are shown independently of burrow connections and recover online. The worker promotes a shell only with its complete JS/WASM bundle, preserves old open tabs, and bypasses downloads/non-shell resources. Real Chromium service-worker tests cover update, offline, recovery, and native exclusion. TLS remains deferred
- [~] Web admin routes: config, accounts/classes, moderation, monitors (federation/radio panels as they land) — `ui-web`: `/admin` route with config (ConfigGet/Set), accounts & classes (AccountList/Set, ClassList/Set), moderation (Kick/Broadcast); `AdminCommand`/`AdminEvent` + `admin_command_to_frame`/`frame_to_admin_events` in `wire.rs` (host-tested, mirrors FILE family), `AdminState` reducer, nav gated on `is_admin`, `MockClient::dispatch_admin`. Plus `WsClient` **reconnect/backoff** (pure host-tested `backoff_delay` capped-exponential+jitter, `ConnState` Connecting/Online/Reconnecting/Offline surfaced in StatusBar) and live **FILE-family dispatch** over the socket. Federation/radio monitor panels land with those subsystems **Console rebuilt (0.216.0, `docs/design/admin-console.md`)**: the burrow describes its own settings (`ConfigDescribeRequest`/`ConfigDescription`, ADMIN 15/16: value, default, kind, choices, live or restart, secret, read-only, derived from `set_key` itself), so the console shows all 102 keys instead of 14 guessed ones; `ui-web::admin_catalog` (sections, labels, descriptions; a host test holds it to the server's key list in both directions), `ui-web::admin_settings` (staged edits, Reset to the burrow's default, per-row outcomes) and `ui-web::admin_view` (sections navbar at `/admin/:section`, switches, dropdowns, number fields that say `1 GiB` and `2 hours`, one save bar, Cmd-S); an older burrow falls back to key-by-key reads with no Reset. **A config change is now written to `burrow.toml` or refused** (it was memory-only: "saved, restart to apply" lost the change); `--config` defaults to `burrow.toml` in the data dir as the help always said; published themes persist too; credentials are never read back and never reach the audit log. **Surfaces are live (0.217.0)**: `apps/server/src/surfaces.rs` reconciles telnet, finger, HTTP, NNTP (reader, TLS, feed, feed TLS), radio (with its rotation pumps), the DJ source ingest, Hotline, FidoNet and the feed poller against the live config at boot and after every config change, before the change is acknowledged; a surface that cannot bind is reported (`SurfaceStatusRequest`/`SurfaceStatus`, ADMIN 17/18; `burrow ctl surfaces`) and retried on the next change, never fatal; the console shows "Listening on …" or why not beside each switch. 33 keys moved from restart to live. A reset takes its line out of `burrow.toml`. The client's hand-kept gateway matrix and liveness table are gone. **People (0.218.0)**: `AccountCreate`/`AccountPasswordSet`/`AccountTotpReset`/`InviteListRequest`/`InviteList`/`InviteRevoke` (ADMIN 19-24, `apps/server/src/handlers15.rs`); one standing rule for every account operation (below yourself, never yourself, roles up to your own) now also on `AccountSet`, which had none, and `ClassSet` refuses a capability the operator lacks; a password reset or a disable revokes saved sign-ins and closes open sessions. Console: `ui-web::admin_people` + `admin_people_view` (accounts with inline role/class/reset/disable, a new-account form with a generated password, classes as named capabilities held to the server's bits by a host test, invitations with copy and withdraw); each action reports its own outcome (replies are tagged by `AdminCommand::tag`). Still open: account delete, account search. **Boards (0.219.0)**: `BoardUpdate`/`BoardDelete` (BOARD 15/16), slug rules on create, delete only when empty; console pane `ui-web::admin_boards_view` (tree in reading order, new board or category with an address suggested from the title, rename/describe/remove in place); a removed post keeps its place and says so, and its author or a board moderator gets a Remove control (`PostDelete` existed and was never offered). Management replies are now paired with their request by id, not arrival order, so BOARD and FILE replies report through the same tagged path as ADMIN ones. **File areas (0.220.0)**: `AreaUpdate`/`AreaDelete` (FILE 27/28; delete only when empty; a title is required), console pane `ui-web::admin_areas_view`; in Files, holders of `FILE_MANAGE` get New folder (with the drop-box choice), Describe and Remove on the selected node (`FolderCreate`, `SetMetadata`, `NodeDelete` existed and were never offered). The welcome screen's "here now" counts people, not sessions, and the client's roster collapses one person's sessions into one row (the desktop app, its background download connection and a second device used to read as three of the same name). **Moderation (0.221.0)**: the audit log over the wire (`AuditListRequest`/`AuditList`, ADMIN 25/26, `AUDIT_READ` finally checked); console pane `ui-web::admin_moderation_view` with the report queue by state (claim, resolve, dismiss with a note; `ReportList`/`ReportResolve` existed unsurfaced), every session with Disconnect by name (the client carries sessions as well as the collapsed roster), broadcast, the hash-deny list (add and allow again; `DenyHash*` existed unsurfaced) and the audit log as sentences; the old panel with its typed session-id box is gone. **Peers and backups (0.222.0)**: federation peers (`PeerListRequest`/`PeerList`/`PeerApprove`/`PeerRevoke`, ADMIN 47-50), trusted origins (`OriginListRequest`/`OriginList`/`OriginPin`, 51-53) and backups (`BackupListRequest`/`BackupList`/`BackupCreate`/`BackupMade`/`BackupVerify`/`BackupVerified`/`BackupDelete`, 54-60; Admin role required) over the wire, `apps/server/src/handlers16.rs`, sharing `federation::approve_peer`/`revoke_peer` with `ctl`; new `backup_dir` config key (live; `ctl backup` with no destination uses it); console panes `ui-web::admin_peers_view` and `admin_backups_view` (`admin_federation` model). Restore stays offline. **Files: rename and move (0.223.0)**: `NodeRename`/`NodeMove` (FILE 29/30; `FilesRepo::relocate_node` rewrites a subtree's paths in one transaction, by characters); in Files the selected node gets a Name field and Move, which picks it up so the person can walk to the folder and put it down (`FilesState::carrying`, `cannot_put_down_here`). **Boards sit where they are put (0.265.0)**: `BoardMove` (BOARD 17) and a `position` column (migration 0016, backfilled in the order each burrow already read); `BoardsRepo::move_after` renumbers a board’s siblings in one transaction (by the burrow’s own spelling of a slug, which is NOCASE), a board made later is read after the ones already there, and `BoardsRepo::all` reads the burrow as a tree — each board, then what is inside it — because a position counts only among the boards under one parent. The client cache kept boards by slug, throwing that arrangement away, so it keeps the burrow’s order now. Found on the way: asking the socket for something from inside a reply (a pane that mounts on news and asks for what it shows — a console open while the burrow restarts) panicked on its `RefCell` and, on wasm, left every later send on that socket dead. Every `WsClient` dispatch now goes through `when_free`, which sends at once when the socket is free and a tick later when a reply is being handled. In the console’s board tree, Move up and Move down on each board. **What a board keeps, from the console (0.266.0)**: `BoardUpdate` carried `max_threads` from 0.219.0 and no listing carried it back, so retention could only be set blind. `BoardKeepingRequest`/`BoardKeeping` (BOARD 18/19, `BOARD_MODERATE`) say what each board keeps and how many threads it holds; the board editor has “Threads to keep” beside the title, with a line that says what that means and what is there now, and the burrow prunes as it always did. Saving sends the number only when the operator moved it — left empty it stays as it is, a listing arriving mid-edit no longer takes back what was typed, and a refused save leaves the number where it was typed rather than quietly putting the old one back. Review-born rules: a tagged reply that names a burrow is only for that burrow, checked once in `fold_admin_reply` before anything folds it (every live session folds admin events into the one console model, so `AdminState::apply` deliberately does not write this map); asked-and-unanswered is its own state (`Keeping::Asking`), since saying a burrow does not keep track while the question is in flight is a lie an operator would act on, and a pane asks again on each sign-in (`Session::ready`) because a socket coming back is not an answer coming back; the audit line says what retention became, not just that the board was edited; and a post is minted under the board’s own spelling, since a slug matches case-insensitively and a post stored under the caller’s casing is invisible to that board’s threads, counts and retention. Federated projections now use the local board’s canonical slug while preserving signed event bytes; migration 0018 repairs existing known-board projections, and case-insensitive relay matching keeps them moving between burrows (RH-50). Still open across the program: restore or download of a snapshot from the console, moving between areas. **Removing an account, and finding one (0.264.0)**: `AccountDelete` and `AccountFindRequest` (ADMIN 63/64). Removing was nowhere — Hotline's "delete user" disables, and `ctl` had none — so an operator could only ever disable. It keeps the standing order and a burrow's last administrator, signs the person out, and takes what hangs off them (personas, sign-ins, two-factor, keys, buddies, blocks, read marks) by foreign key; what they wrote stays, under the name they wrote it with, and the login is free again. In People: Remove with a question that says exactly that, and the filter box now asks the burrow as well when it holds more accounts than are loaded, so one person can be found among thousands. **Holding content back, from the console (0.263.0)**: `QuarantineSet`/`QuarantineClear` were writable over the wire from wave 13 and readable from nowhere, so no console could say what a burrow was holding. `QuarantineListRequest`/`QuarantineList` (ADMIN 61/62, `MODERATE`) list what is held with the reason, the moderator and when; `ModerationService::held` reads the store the in-memory mirror is warmed from. Moderation pane: a "Held back" group with Let it through, and on a report about a post or a file, Hold it back (the note, else the report’s own reason, goes on the record) or Let it through when it is already held. Holding a person is what disabling an account is for, and a direct message is held from nobody, so neither is offered. **Uploads of any size (0.224.0)**: `upload_max_file_bytes` (default 50 MiB, live) beside the per-person space in Files & transfers; one gate for every surface that files bytes (`apps/server/src/upload_gate.rs`: the app's inline and ticketed uploads, Hotline, ZMODEM), at the declared size and again on the bytes that arrived; `UploadLimitsRequest`/`UploadLimits` (FILE 31/32) tell the client both limits and what the person has used. The browser (and the desktop app's webview) sends files over 640 KiB as ticketed uploads: hashed in slices, 256 KiB chunks four in flight, a queue row with progress and Cancel (`WsClient::call` awaits a reply by request id); the Files toolbar says what may be uploaded, and a refusal says which limit and by how much. **Pulls between burrows, the burrows' side (0.225.0)**: Kevin's review answered (B only, approved peers, filed under the person, trees recreated); `PullGrantRequest`/`PullGrantIssued`, `RemotePull`/`RemotePullAccepted`, `RemotePullCancel`, `RemotePullStatus` (FILE 33-38), grants in `rabbithole_federation::pull` (`rhp-fed-pull-grant-v1`), `apps/server/src/s2s.rs` riding each live federation session's bulk streams, provenance on `file_nodes` (migration 0013), four live settings under Federation & feeds. QUIC connections keep alive every 10 s (federation sessions used to idle out every 30). **Send to another burrow, the app (0.226.0)**: on a selected file and on folder rows, when the person is signed in here and on another live burrow; `ui-web::send_view` walks the other burrow's areas and folders by awaited requests (its Files view untouched), asks here for the grant with the other's server key (now kept from its handshake), hands it over, and follows the pull on the destination's queue with Cancel; the ending is one toast, not repeated on a reconnect's replay; `ui-web::send` holds every sentence. **Between burrows that are not peers (0.227.0)**: each operator opts in at their end (`s2s_grants_to_any`, `s2s_pull_from_any`, both off); grant version 2 carries the source's certificate fingerprint and up to four addresses (`advertise_host`, then the host the app reached it at, sent with `PullGrantAsk`, FILE 39); the destination resolves each once and refuses private ones unless `s2s_private_addresses` (`rabbithole_net::reach`), dials the source's client QUIC port pinned to that certificate, proves its server key there and opens an account-less pull session (`PullSessionOpen`, SESSION 15) that serves only that grant's pull streams until its grace ends, one session per grant, opened only while it stands. Peers keep grant version 1, so 0.225/0.226 peers still interoperate. Dialing out is charged to the person's transfer budget and pull slot first. The QUIC listener now shakes hands on a task per client (one silent client used to hold the port for everyone). **The swarm leg (0.228.0)**: with `s2s_swarm_sources` at the source and `s2s_swarm` at the destination (both off), the destination asks the source, per file of 2 MiB or more, on a stream that opens with an empty frame (`PullStreamAsk::Sources`), for its visible seeders and an `S2sCapToken` (`rhp-swarm-cap-s2s-v1`, naming the destination's key; seeders accept it beside a person's token), fetches the pieces with the swarm engine through the address guard into its own staging file, and takes from the source whatever the swarm does not give (after one failure, the rest of the send). The source offers its swarm only on grants whose person may use it, and refuses a grant's files once that person's account is disabled. Dropping a swarm fetch now stops its workers. Not built: a per-send choice for the person.
- [~] Tauri v2 desktop: core in-process, QUIC transport, native notifications, tray/menubar presence, `rabbit://` deep links, auto-update — **design + scaffold plan landed** (`docs/design/client-experience.md` full client experience; `docs/design/desktop-shell-scaffold.md` the version-verified Tauri v2 `apps/desktop` recipe — tauri 2.11.5, standalone crate excluded from the workspace, `beforeDevCommand` cwd'd into ui-web, `csp:null`). Two multi-agent studies (design synthesis + Tauri scoping) grounded in the shipped ui-web + swarm/blobs/identity primitives. Toolchain verified on-machine (tauri-cli 2.9.6, Xcode 26.6, iOS + Android rust targets; Android NDK unset). Build order: WarrenState refactor (multi-session) → rail shell → identity/presence/People → unified swarming Transfers → unified Messages → native Tauri chrome. **Desktop shell scaffolded + building (0.105.0)**: `apps/desktop` is a real Tauri v2 app wrapping the shipped ui-web SPA — `cargo build` + `cargo tauri build --bundles app` both green, producing `RabbitHole.app` (25 MB arm64 mach-o that *embeds* the live wasm SPA via `generate_context!`; launches without a scaffold panic). Standalone crate (own `[workspace]` + `Cargo.lock`) so the root `cargo build --workspace` stays at 35 packages, no tauri/webkit. Full icon set generated from the burrow-hole PWA icon. **iOS shell scaffolded + verified-compiling (part of 0.105.0)**: `cargo tauri ios init` generated `apps/desktop/gen/apple` (Xcode project via xcodegen, `project.yml`, Podfile, Sources), and the Rust core **cross-compiles clean for iOS** (`cargo build --lib --target aarch64-apple-ios-sim` → `librabbithole_desktop_lib.a`, tauri 2.11.5 + tao + wry, 41.75s) with `trunk build` producing the wasm frontend. The `mobile_entry_point` split means the same crate is the iOS app. **Two toolchain gotchas solved (documented in `desktop-shell-scaffold.md`):** (1) asdf's `ruby` shim has no version set → `gem list` exits 126, breaking `pod install`; fix = run tauri ios commands with `PATH="$HOME/.cargo/bin:/opt/homebrew/opt/ruby/bin:/opt/homebrew/bin:$PATH"` (homebrew ruby/cocoapods for gem/pod). (2) that PATH must keep `~/.cargo/bin` FIRST or homebrew's rustc (no wasm32 std installed) shadows rustup and the frontend `trunk build` fails E0463. Remaining to *run* on a simulator: download the iOS 26.5 simulator **runtime** (only 26.0 installed) via Xcode / `xcodebuild -downloadPlatform iOS`; Android needs `ANDROID_HOME`/NDK. NB: `apps/desktop` is NOT in the root workspace — build it from its own dir (`cd apps/desktop && cargo tauri dev/build/ios build`). Also hardened the desktop Tauri config (0.105.0 follow-up): explicit CSP + `withGlobalTauri:false` (from the automated security review).

**Wave A — WarrenState refactor foundation landed (0.106.0)**: the single-session `AppState` is now multi-session-ready. Extracted the seven per-connection fields (`state, files, is_admin, live, server_theme, ws, client`) into a `Copy` `Session`; `AppState` now holds `sessions: StoredValue<Vec<(ServerId, Session)>>` + `focused_id: RwSignal<ServerId>` + a `focused() -> Session` accessor, keeping the unified/warren fields (toasts, palette, servers, theme, radio, admin…) at the top. Migrated ~95 callsites (`app.state`→`app.focused().state`, etc.) across app.rs + components.rs. Dependency-free (`Vec<(ServerId, Session)>`, no indexmap). **No visible change** — seeds one session (`ServerId::local`); focus never changes yet. Verified: 228 host tests + clippy + wasm green, plus a live burrow smoke (connect→Online, lobby chat sends+renders, boards list live) confirming the transport sinks still fold into the focused session. **Wave B slice 1 — the burrow rail landed (0.107.0)**: the persistent left **rail** is now part of the app shell (`BurrowRail` in app.rs; `.rh-shell`/`.rh-rail` CSS). Renders the unified home mark (burrow-hole → /lobby), the connected-burrow tiles from `AppState::burrow_tiles()` (accent-tinted squircles, first-letter glyph, active pill; clicking calls `AppState::focus()` + navigates), and a "+" add-burrow tile (→ Looking Glass). `sessions` is now a reactive `RwSignal` so the rail re-renders as sessions are added; hidden on the login route via a reactive `class:rh-rail-hidden`. Uses `--rh-accent` for now (the warm `--rh-brand` split is a later visual slice). **Verified live**: rail hidden on `/`, visible in-app with the home + "Demo" (local session) + add tiles, and the lobby/nav/roster render intact beside it. 228 tests + clippy + wasm green. **Wave B slice 2 — genuinely multi-server (0.108.0)**: `connect_live` now spawns a **distinct `Session` per endpoint** (`AppState::ensure_session` — keyed by the dial URL, focuses it, keeps the offline "local" demo + any other burrows), and the App shell **remounts `<Routes>` on `focused_id` change** so every view re-binds the newly-focused session's signals (no per-view churn; the URL stays on `<Router>`). Clicking a rail tile calls `focus()` → the place swaps. **Verified live**: connected to a real burrow → the rail showed 2 server tiles (Demo + `127.0.0.1`); the live tile is **Online** with the real "General" board; switching to the **Demo** tile flips the header to **Offline** (the independent mock session); switching back returns to **Online**. Two independent sessions, each its own connection + place. 228 tests + clippy + wasm green. **Wave B slice 3 — rail tiles show the burrow name (0.109.0)**: added a `name: RwSignal<Option<String>>` to `Session`, set from the `Connected` handshake's `server_name`; `burrow_tiles()` prefers it (then a published theme name, then the endpoint host). The `<For>` key now includes the name so the tile re-renders when the name arrives. **Verified live**: the tile went from `1:127.0.0.1` to `A:An Unnamed Burrow`. **Wave B slice 4 — per-tile connection-health dots (0.110.0)**: each rail tile now carries a small status dot (green online / accent pending / muted offline) from that session's `ConnState` (`burrow_tiles()` returns it; `ConnState` gained `Hash` for the `<For>` key). See every connected burrow's health at a glance. **Verified live**: Demo tile → gray (off), live burrow → green (on). Also confirmed the offline mock demo is intact after the multi-session refactor (mock sign-in → seeded lobby/directory/boards all present). **Wave C sub-slice 1 — global status fanned to all burrows (0.111.0)**: a single presence control in the header sets the user's status (Online / Away / Invisible-Cheshire) and **fans it to every connected burrow at once** — the warren-layer "set once, applies everywhere". Added `wire::presence_set_request` (+ host test) and `WsClient::set_presence` (sends `PresenceSet`, the one send verb the SPA lacked; server handles it at handlers3.rs:78); `AppState.presence` (warren-layer signal) + `set_presence()` loops every live session's socket; a newly-connected burrow **inherits** the current status on sign-in. **Verified live**: the control defaults Online, set to Away → server accepted the frame (no CommandFailed). 229 host tests + clippy + wasm green. **Wave C sub-slice 2 — richer roster (0.112.0)**: upgraded the SPA roster from `who: Vec<String>` to `who: Vec<Presence>` (new `state::Presence { screen_name, state, transport }`). `frame_to_who` maps `UserSummary` → `Presence` (was flattening to bare names); `PresenceDelta::Joined` now carries a `Presence`; the mock `refresh_who` maps handles → Online presences; `WhoList` renders each user with a status dot (green online / amber away / muted idle-off). `PresenceState` gained `Hash` (for the `<For>` key; additive, wire golden unchanged). **Verified live**: the who-list shows `alice` with a green online dot. This is the data the People view + per-user badges need. 229 host tests + clippy + wasm + proto golden green. **Wave C sub-slice 3 — the People view (0.113.0)**: the marquee — your people coalesced across *every* connected burrow. `state::{Person, merge_people}` (pure, host-tested: unions each session's roster, de-dups by screen name, lists the burrows each person is on, prefers an Online sighting); `AppState::people()` reactive accessor; a `People` component at `/people` (rows = presence dot + name + the burrows they're on) reached from a new brand-ember ☺ **unified rail tile**. **Verified live**: clicked the rail People tile → `/people` showed `alice @ An Unnamed Burrow`. With N burrows a person coalesces across them. 230 host tests + clippy + wasm green. **The warren layer now has: one status control fanned everywhere (C.1) + a presence-rich roster (C.2) + cross-server People (C.3).** **Wave D sub-slice 1 — the unified Transfers manager (0.114.0)**: every download/upload across *all* connected burrows in one warren-layer view. `AppState::all_transfers()` (reactive; each session's `FilesState.transfers` tagged with its burrow name); a `Transfers` component at `/transfers` (direction arrow + name + burrow + a reused `rh-bar` progress bar + `rh-badge` status) reached from a new brand-ember ↓ **rail tile**; `TransferStatus` gained `Hash` for the `<For>` key. **Verified live**: uploaded + downloaded `note.txt`, the Transfers view showed `note.txt | An Unnamed Burrow | Done` at 100%. **The rail now carries the full unified layer — Home · People · Transfers — above the server tiles.** **Wave D sub-slice 2 — content-keyed transfers (0.115.0)**: each transfer now carries its **blake3 content hash** (`Transfer.hash`, from the downloaded node's `blob_id`), and the Transfers manager shows it (`blake3 8d12a2ad…212b`) plus a **source pill** structured for N sources (`1 source · <burrow>` today). This is the design's content-addressed marquee *foundation* — the hash is the swarm de-dup key. **Verified live**. **Honest boundary**: the web download is a single-server inline fetch, so the live **multi-source roster + Bao chunk map** (pull from many servers + peers at once) genuinely needs the **native (Tauri) in-process swarm engine** (`crates/swarm`/`crates/blobs`) or the **WebRTC peer gateway** — flagged as critical-path deps in the design doc; the pill's tooltip says so. **Native swarm backend — Slice 1 landed (0.116.0)**: a 6-agent scoping study (`docs/design/native-swarm-backend.md`) mapped the whole integration and found the native RHP client is *reuse not construction* (`rabbithole-core` feature `native` already has `Client::connect`/`swarm_find`/`swarm_ticket`; `rabbithole-swarm` has `fetch_swarm_resumable`), the one real crate change is a small additive scheduler progress hook, and the biggest risk is the wasm↔Tauri IPC seam (deferred to its own spike). **Slice 1 (done, fully headless)**: added `rabbithole-swarm` + `tokio` path deps to `apps/desktop` — proving swarm/quinn/rustls **links alongside tauri/wry/webkit** (the dep-tree-bloat risk, falsified by one `cargo build`) — and a `#[tokio::test]` (`apps/desktop/tests/swarm_core.rs`) that stands up 3 localhost `PeerServer`s + a dead endpoint and fetches a 4-unit blob **byte-exact through the real desktop build** (multi-source work-stealing + failover + Bao verify + `.rhstate` cleanup), 3/3 reliable. The in-process swarm core works. **Slice 2 landed (0.117.0)** — source discovery in-process. Added `rabbithole-core` (feature `native`) + `rabbithole-proto` path deps to `apps/desktop`, **passing the second dep-tree gate** (core's `store-client`/rusqlite links with tauri, `cargo build` exit 0). `apps/desktop/src/swarm.rs`: `run_swarm_download(client, root, size, dest)` orchestrates `swarm_find` → `sources_from_list` (pure filter: only `SourceInfo` with both endpoint + cert_fp become dialable `SourcePeer`s; coordinator-only → `NoPeerSources` origin fallback) → `swarm_ticket` → `fetch_swarm_resumable`. The filter + size-derivation are host-unit-tested (3 tests); the orchestration compiles against the real `Client` API and composes pieces already tested individually (`swarm_find`/`swarm_ticket` in `apps/server/tests/e2e_w53.rs`, the fetch in Slice 1). **Slice 4a landed (0.118.0)** — the live progress hook (the one genuine crate change, fully headless). `crates/swarm/src/scheduler.rs` gained `UnitDone {endpoint, offset, done_units, total_units}` + `ProgressSink` + `fetch_swarm_resumable_with_progress`: the worker emits a `UnitDone` under the lock right after each unit's `persist()`, so a UI gets one event per verified unit as the swarm fills. Existing `fetch_swarm`/`fetch_swarm_resumable` are unchanged thin wrappers (a private `resumable(…, progress: Option)` inner), so all swarm consumers (burrow, rabbit cli) still build + the 38 swarm lib tests pass. `apps/desktop/src/swarm.rs`: `run_swarm_download` now takes an `emit: impl FnMut(SwarmEvent)` and forwards `Opened`/`Chunk`/`Done` by draining the progress channel while the fetch runs on its own task. **Headless-verified**: `apps/desktop/tests/swarm_core.rs::progress_hook_streams_every_unit` asserts one event per unit, `done_units` climbing to total, real endpoints. **Slice 3 written (0.119.0)** — the wasm↔Tauri IPC hello-world spike (the one non-headless risk, isolated per the scoping). `apps/desktop/src/lib.rs` now builds the window in Rust (`WebviewWindowBuilder`, config `app.windows` emptied) carrying a `NATIVE_SHIM` init script that exposes `window.__RH_NATIVE__ = {invoke, listen}` over the always-present `window.__TAURI_INTERNALS__` — **keeping `withGlobalTauri: false`** (honors the security review; the low-level internals bridge works regardless). Added a `ping` command + a `test://tick` emit ~1s after launch; the shim self-tests both and logs to the devtools console. Granted `core:event:allow-listen`/`allow-unlisten`; added tokio `time`. **Verified as far as headless allows**: clean `cargo build`, 3 lib tests pass, and a full `cargo tauri build --debug` produced a valid `RabbitHole.app` (proving the new window model + config + capabilities + SPA packaging). The **runtime round-trip needs a GUI** — `cd apps/desktop && cargo tauri dev`, then watch the console for `[rh-native] invoke ping -> pong: slice-3` and `[rh-native] event test://tick -> …` (steps in `docs/design/native-swarm-backend.md`). **Slice 4b written (0.120.0)** — the Tauri command/event surface. `apps/desktop/src/transfers.rs`: a `TransfersManager` managed state (`Mutex<Option<Client>>`) + commands `native_available`, `connect_native(endpoint, fingerprint)`, `swarm_start_download(transfer_id, root_hex, size, dest)` — the latter wraps the tested `run_swarm_download` and emits `swarm://event` (a `SwarmEvent` flattened under a `transfer_id`) as each unit lands. `SwarmEvent` gained `#[derive(Serialize)] #[serde(tag="kind", rename_all="snake_case")]` — **the wire contract the wasm SPA deserializes, locked by a host test** (`serializes_to_the_wire_contract`); `parse_root` (hex→[u8;32]) is host-tested too. Registered in `lib.rs` (`.manage` + `generate_handler!`). 5 lib tests + clippy green; compiles against tauri + the real `Client`. **The command bodies are already-tested logic; only their IPC *delivery* awaits the `cargo tauri dev` round-trip.** **Slice 5a landed (0.121.0)** — the ui-web translation bridge (the host-verifiable core of the ui-web wiring). `crates/ui-web/src/wire.rs`: `SwarmWireEvent` (a `serde(tag="kind")` **mirror** of the desktop crate's `SwarmEvent`, deserialized from the `swarm://event` JSON with `transfer_id` flattened in) + `swarm_event_to_file_events(ev, size)` — a **pure** translator that maps each native swarm event to the *same* `FileEvent`s the WebSocket path emits (`Opened`→`TransferOpened`, `Chunk`→`ChunkReceived` with the unit's byte offset + derived len, `Done`→∅). **Host-tested**: deserializes the exact wire JSON and drives a `Transfer` to 100% through the real `FilesState` reducer (231 tests + clippy + wasm build green). So native progress reuses the existing Transfers UI verbatim — zero reducer/view forks. **Slice 5b landed (0.122.0) — the native swarm path is wired end-to-end.** `crates/ui-web/src/native.rs` (wasm-only): `native_available()` (reads `window.__RH_IS_NATIVE__`), `start_swarm_download` (invokes `swarm_start_download` over `window.__RH_NATIVE__.invoke`), `install_swarm_listener` (subscribes to `swarm://event`, JSON→`SwarmWireEvent`, folds into the focused session's Transfers via the tested translator; the `Closure` is `forget()`-leaked to stay alive). `AppState::download` now branches: in the native shell a content-addressed file resolves its blake3 root + size + name, seeds a local Transfer, and kicks the swarm fetch; else the WS inline path. `App()` installs the listener once. Desktop `swarm_start_download` now takes `name` and resolves the OS downloads dir, with a host-tested `sanitize_name` guarding path traversal. **Compiles across all three targets** (desktop, ui-web host, ui-web wasm); 231 ui-web + 6 desktop tests + clippy (host & wasm) green. **The full path exists**: click download in the desktop app → resolve who has the blob → pull from many peers at once → verify every block → live progress into the existing Transfers UI. **Runtime confirmation still needs the GUI** (`cd apps/desktop && cargo tauri dev`, verify the Slice 3 console lines, then a real multi-source download). Deferred: concurrent downloads + mid-fetch abort (split find/ticket from the lock-free fetch, add `swarm_abort`); per-source Bao verify-error surfacing. **Native download Rust path locked E2E (0.123.0)**: `apps/server/tests/e2e_w53.rs::multi_source_swarm_fetch_via_coordinator` — three peers seed one file on a **live Burrow**, the fetcher discovers all three via `swarm_find`, filters to dialable `SourcePeer`s, gets a ticket, and pulls the file **multi-source** with `fetch_swarm_resumable`, byte-exact, all four units served exactly once (3/3 reliable; the Rust half of `run_swarm_download` against a real coordinator, not just composed pieces). The "needs a GUI" gap is now **only** the wasm↔Tauri IPC round-trip (Slice 3); everything else in the native swarming path is tested. 230 tests + clippy + wasm green. **Portable identity + the "You" hub landed (0.124.0)**: a persisted Ed25519 keypair that names *you* across every burrow, independent of your per-server handle. `crates/ui-web/src/identity.rs`: `Identity` (deterministic `from_seed`, `public`/`public_hex`/`fingerprint` = blake3(pubkey)[..8] hex, host-tested incl. an Ed25519-derivation guard) + wasm-only `load_or_create` (browser CSPRNG seed via web-sys `Crypto`, persisted hex in `localStorage`) + a secret-free `You { fingerprint, public_hex }` view. Used `ed25519-dalek` + `blake3` + `hex` directly (not `rabbithole-identity`, whose argon2/totp tree is too heavy for wasm) with `getrandom` "js" — **wasm build clean**. `AppState.you` loaded once in `App()`; a `You` component at `/you` (gradient fingerprint badge + fingerprint + public key + the "how your people find the real you" note) reached from a new ✦ **rail tile** (the unified stack is now Home · People · Transfers · You). **Verified live**: the hub shows a 16-hex fingerprint + 64-hex key, and **persists across a full page reload** (same fingerprint → the localStorage seed round-trips). 233 host tests + clippy + wasm green. Next for verified-key People de-dup: carry the pubkey on presence/profile (the `Option<PublicKey>` proto delta) so `merge_people` can coalesce by key instead of handle. **Verified-key People de-dup landed (0.125.0)**: `merge_people` now coalesces by *verified identity key* when a burrow reports one, else by handle — so the same human under different handles per burrow merges into one row (adopting their Online handle), while two strangers who both picked "rabbit" stay distinct. Threaded `key: Option<String>` (pubkey hex) through `state::Presence` + `state::Person`; the People view shows an accent ✓ **verified mark** whose tooltip fingerprint uses a shared `identity::short_fingerprint` (host-tested to equal the You hub's own `Identity::fingerprint`, so your key and a remote person's read identically). Two new host tests (`merge_people_coalesces_by_verified_key_not_handle`, `short_fingerprint_matches_the_you_hub`); 235 tests + clippy + wasm green. **Verified live**: the offline demo seeds "rabbit" with your portable identity key → People shows `rabbit ✓` with tooltip `98edd754…` matching the You hub fingerprint exactly. The de-dup *intelligence* is complete + tested; the remaining piece to light it up over real burrows is the **additive proto delta** — append `Option<[u8;32]>` pubkey to `presence::UserSummary` (+ `directory::ProfileCard`), carried from the client (via `Hello`/register) → stored per-session → echoed in the who-list → read in `frame_to_who` (currently `key: None`). That's a proto+server slice (with the wire-golden discipline + server tests), teed up. **Proto delta landed (0.126.0)** — the additive contract for it: appended `pubkey: Option<[u8;32]>` to `presence::UserSummary` (+ `with_pubkey` builder) and `client_pubkey: Option<[u8;32]>` to `hello::Hello` (+ `with_pubkey`), both `#[non_exhaustive]` so `::new` callers are untouched and default to `None` — the additive-only-within-version pattern. Two round-trip tests (`hello_carries_optional_pubkey`, `user_summary_carries_optional_pubkey`); **wire-golden registry unchanged** (name-keyed, appended fields add no triple); full workspace builds. **Slice B landed (0.127.0) — verified-key de-dup is live over the wire.** The pubkey now flows end to end: server captures `Hello.client_pubkey` → `SessionCtx.pubkey` → `PresenceEntry.pubkey` (server-core; legacy hotline/telnet bridges pass `None`) → the who-list's `UserSummary.with_pubkey` (all 3 build sites); the web client's `wire::hello_request(id, pubkey)` presents `identity::load_or_create().public()` at handshake (ws.rs), and `frame_to_who` + the Joined delta read `u.pubkey.map(hex::encode)` into `Presence.key`. Core `Client::connect_with_identity` added (existing `connect` delegates with `None`, so all 92 call sites untouched) — native/CLI parity + testability. **Verified**: a new server E2E (`e2e_w53::who_list_carries_the_handshake_pubkey`) asserts a keyed client's key is echoed in the who-list while a bare client shows `None`; **live** against a real burrow, People showed `alice ✓` with tooltip `78ab2982…` matching the You hub fingerprint exactly. Proto round-trip + golden + 235 ui-web + server-core (98) + burrow (51 + E2E) + core (6) + clippy + wasm all green. **Verified-key People de-dup is complete end to end.** **Welcome sheet landed (0.128.0)**: the SPA had been *dropping* the post-auth `Welcome` frame (MOTD + agreement); now it surfaces it as a non-modal sheet. Added `Event::Welcome { motd, agreement }` to the api enum (additive, `#[non_exhaustive]`), decoded in `frame_to_events` (session type 40); `UiState.welcome: Option<state::Welcome>` folded in `UiState::apply` (only raised when there's content — blank MOTD stays silent; agreement-only still raises) + `dismiss_welcome`; a `WelcomeSheet` component (reactive over the focused burrow via a new `AppState::focused_tracked`) rendered atop the shell main pane — accent-bordered, "Welcome to <burrow>" or "Before you enter" with an Accept/Got-it action. Host-tested (`welcome_event_raises_and_dismisses_the_sheet`); **verified live** (set `motd` via `ctl config-set motd …` → the sheet showed the MOTD non-modally over the lobby, dismiss cleared it). 236 ui-web + workspace + clippy + wasm green. **Reconnect-on-launch session persistence landed (0.129.0)**: the login screen now remembers the burrows you've signed into. New `recent.rs` — a pure, host-tested reducer `add_recent` (dedup by endpoint, most-recent-first, cap 8) + wasm-only `load`/`remember` persisting `[{endpoint, handle}]` (never the password) to `localStorage`. `connect_live` calls `recent::remember` on the `Connected` event (only successful sign-ins); the `Login` component seeds server + handle from the most recent, defaults **live** on when there's a burrow to return to, and renders **RECENT** quick-reconnect chips (`handle @ host`) that one-tap prefill + arm live. Two host tests (`add_recent_dedups_…`, `add_recent_caps_the_list`); **verified live**: connect → reload → server `ws://127.0.0.1:4674` + handle `alice` prefilled, live checked, chip `alice @ 127.0.0.1:4674` shown; localStorage held the JSON. 238 ui-web + clippy + wasm green. **Wave B polish is complete.** Still pending (smaller): native/CLI actually presenting a key (the hook is `connect_with_identity`); full auto-auth reconnect (needs a stored resume token — deferred, password re-entry for now); the `cargo tauri dev` bridge check to close the native swarm loop.
- [ ] Server native GUI wrapper (systray + bundled daemon)
- [x] Playwright E2E suite and CI (RH-64) — `e2e-web/` runs the real Trunk/WASM app against isolated local burrows in Chromium: guest authentication and lobby, signed themes, direct-route reload/session resume, and initial load plus immediate reload under production-default rate limits (RH-46). The CI web job derives the wasm-bindgen CLI version from `Cargo.lock`, builds the actual SPA and server, uses locked npm dependencies and Playwright Chromium, and refuses to silently skip artifact-backed tests. Failure traces, screenshots, browser/server/build logs and the HTML report are retained; normal teardown plus a process-verified cleanup step stop fixture servers. Manual smoke testing still supports `BASE_URL`/`WS_URL`; `PW_CHROMIUM` can select a local browser. See `e2e-web/README.md`.

## Wave 9 — Federation & discovery
*Depends on: W3 (+W4/W5 for catalogs/swarm; W8 admin UI helpful)*

- [x] S2S QUIC channel; immutable origin/key handshake + admin approval — federation protocol v2 signs `rhp-fed-s2s-auth-v2‖keys‖length-prefixed origins‖nonces`; `federation_origin` is restart-only and required when federation is enabled. Unknown origin/key tuples stay **Pending** until audited `peer-approve KEY [ORIGIN]`; versioned tuple approvals persist across restart, are re-checked on every frame, and `peer-revoke` closes active sessions. Protocol-v1 and legacy key-only approvals fail closed. See `docs/protocol/federation.md`.
- [x] Board flood-fill with authenticated origin provenance — `MT_EVENTS` origin keys are selectors only and cannot establish authority. Direct approved peers install their handshake-proven tuple; indirect origins require explicit `origin-pin`. The versioned provenance store persists before visibility and rejects unknown/conflicting origins, key aliases, cap exhaustion, corrupt/legacy state, persistence failure, and concurrent losing installs. Multi-hop A←B←C remains supported after A independently anchors C; relays cannot squat an unseen origin. Server-key rotation/old-key history remains a future continuity-protocol tranche; periodic re-offer cadence also remains deferred.
- [x] Tombstone/redact propagation (server-sovereign application) — **board Edit/Tombstone follow-ups now flood over S2S** (design: `docs/design/board-followup-flood.md`). `edit()`/`tombstone()` now mint + **store** their signed follow-up events (store migration `0011_board_followups` + `FollowupsRepo`; `tombstone()` gained an actor identity to sign); `BoardService::ingest_event` dispatches all kinds with the **authorization gate** `followup_authorized` (apply only if the same author edits their own post, or the post's home server moderates its own content — a third-party cross-origin forge is refused) + **out-of-order reconciliation** (a follow-up arriving before its target post parks pending, then applies — or drops if unauthorized — when the post lands). New `ServerEvent::BoardEvent` (no unread bump) drives the flood; `federation.rs` offers/pulls/ingests follow-ups on the existing `MT_IHAVE`/`MT_PULL`/`MT_EVENTS` (no wire/proto change — `FedEvent` is kind-agnostic), re-firing pending ones too. Native (`handlers6`) + Hotline (`del_news_art`) call-sites updated. 4 boards unit + 1 store unit + 1 multi-hop e2e (edit + tombstone flood A←B←C, follow-up verifies under the origin's key). The standalone cross-community `redaction::Redaction` model (opt-in "stop serving this hash") stays a separate follow-up
- [~] Ingest defense: dual-sig verify, per-peer rate limits, reputation, auto-defederation, allow/deny lists — `rabbithole-federation::policy` (per-peer token-bucket `RateLimiter`, allow/deny `PeerPolicy`) + descriptor verify done; reputation/auto-defederation pending
- [~] Cross-server identity attestation (`persona@server`, key continuity) — model landed: `rabbithole-federation::attestation` (`PersonaAttestation` home-server-signed under `rhp-fed-attestation-v1` w/ injected-clock freshness + name caps; strict `FedAddress` parser `[a-z0-9-_.]` alnum-edged, DNS-capped; `ContinuityChain` — rotations generation-bound + signed by the *previous* persona key so a server-side key swap fails verification, only the latest link must be fresh; `verify_visitor` challenge-response w/ 16-byte-min challenges under `rhp-fed-visitor-challenge-v1`); 30 tests (84 crate total). Wiring issuance + the challenge flow over the S2S transport is the server slice
- [x] Cross-server file search: signed catalogs, pull fan-out, blake3 dedupe — `rabbithole-federation` primitives (SignedCatalog w/ generation chain, search, dedupe_by_hash, plan_fetch; 54 tests) **wired over the S2S session**: `apps/server/src/fed_catalog.rs` builds the local catalog from publicly-listable files (anonymous-guest SEE|FILE_LIST check, drop-boxes excluded, blake3 blob ids), content-compared rebuilds (stable id when unchanged, generation+prev_id bump when changed), signed catalog persisted so the chain survives restarts; dialer-pull sync on peer connect (announce→compare→fetch→verify against the handshake-pinned key, staleness+tamper+non-approved refused, mid-session revocation cuts fetches); ctl `fed-catalogs`/`fed-search` (federated search w/ provenance + hash-dedupe). 3 e2e tests. Peer-catalog persistence, client-facing RHP search, and live swarm federated sources are follow-ups
- [x] `.well-known/rabbithole/server` (signed descriptor) — `rabbithole-federation::PeerDescriptor` (ed25519-signed under `rhp-fed-descriptor-v2`, including immutable origin, verify + tamper tests) **now served over HTTP**: `apps/server/src/well_known.rs` builds a `DescriptorBody` from live config (immutable origin, display name, advertised `scheme://host:port` endpoints, feature tags per enabled surface, unix-ms `issued_at`), signs it with the burrow's `server_signing_seed` identity, and the embedded HTTP server (`http.rs`) serves it as JSON at `/.well-known/rabbithole/server` (only the exact path resolves; HEAD mirrors GET). Host resolution: new TOML-only `advertise_host` config, falling back to a concrete bind IP and omitting host-based addresses for wildcard binds (no misleading `0.0.0.0`). JSON round-trips the same struct S2S/tracker use in postcard; the sig still covers postcard(body) so JSON transport is verify-neutral. 8 unit + 1 e2e (fetch → verify → self-certifies the burrow identity → origin/name/endpoints/features)
- [x] Looking Glass tracker: signed descriptors, heartbeats, categories, tracker-to-tracker gossip — `looking-glass`: `descriptor` (ed25519 self-certifying `Descriptor` signed as `rhp-trk-descriptor-v1`‖postcard, field caps at verify; registry slots hold the first verified key until TTL expiry — anti-hijack over fast rotation; signed upgrades unsigned, unsigned refreshes but can't rewrite a verified slot, replay-stale timestamps rejected), categories (`LIST cat=<name>` filter + `CATEGORIES` summary verb + categories column), `gossip` (sans-IO `GossipDigest`/`diff→Want`/`GossipBatch`, `RHGS` single-datagram UDP codec on :4656 w/ signed `Announce`, push-pull anti-entropy over static `--gossip-peer`s, via-marker loop safety, gossiped entries under the same lazy TTL); 40 tests. Classic unsigned HTRK flow unchanged. Deferred: digest sampling beyond the 16-entry prefix, signed key-handover
- [~] Directory index (health/uptime, verifiable-not-authoritative); client server-browser UI — tracker side landed: `looking-glass::health` (`HealthLog` — 96×15-min ring anchored at each server's first observation, absolute bucket indices w/ lazy lap-invalidation, expected-heartbeats-so-far judging so fresh servers read 100%, integer per-mille uptime, flap counts fed by the lazy expiry sweep, logs forgotten after a silent 24h) + `INDEX`/`INDEX cat=`/`HEALTH <ip:port>` status verbs (signed-first → uptime → name sort; ASCII bucket sparkline; rows carry signed yes/no + key prefix + descriptor generation so clients verify via gossip `Want` instead of trusting). Uptime is local unsigned bookkeeping, never gossiped (gossiped entries start fresh logs). 59 tests. **TUI server browser landed**: `apps/tui::browser` (total INDEX/CATEGORIES/HEALTH parsers, seq-gated BrowserState w/ filter cycling + in-pane retries, bounded TcpStream edge 3s/5s/512KiB, health sparkline pane, `RABBIT_TRACKER` default, embedded-tracker smoke via the looking-glass lib); uptime labeled tracker-observed. **Web server-browser landed**: `ui-web::servers` (pure host-tested `DirectoryServer` + `browse` filter/rank — reachable→populated→name — + `uptime_label`; 3 tests) + a `ServerBrowser` `/servers` route/nav/⌘K-section: a responsive card grid (presence dot, online count, uptime chip, endpoint, Connect) whose Connect hands the endpoint to the login screen via `AppState.pending_endpoint` (prefilled on Login's next mount). Seeded sample directory until the tracker transport lands; verified in the real browser (ranked cards, search filter, Connect→login-prefill). Offline descriptor verification remains
- [ ] Deploy flagship public **Looking Glass** (project-run, domain TBD); pre-configure in clients (user-removable)
- [x] 3-server CI testnet: partition/rejoin, dupe-storm tests — `e2e_w9_testnet.rs` (4 tests, ~3s): full 6-edge mesh federated search w/ provenance; partition (outer nodes keep serving, dead-node dials bounded) + rejoin on the same data dir (identity + approved peers + catalog chain persist, gen-2 supersedes pre-partition gen-1, no re-approval); 8× announce-storm redials + 5× signed-byte replays are no-op/stale-refused with byte-stable state; mesh-wide stale-generation refusal. Pins today's contract explicitly: one-hop dialer-pull (no transitive relay — flood-fill will break the assertion deliberately). Gaps documented for follow-up: peer catalogs in-memory w/o revoke eviction, no disconnect propagation (no QUIC idle timeout), listener never pulls

## Wave 10 — Syndication (NNTP → FTN → QWK)
*Depends on: W3 (dupe subsystem); W9 helpful*

**NNTP**
- [x] Reader server: CAPABILITIES, GROUP/LISTGROUP, ARTICLE/HEAD/BODY/STAT, NEXT/LAST, POST, OVER/XOVER + OVERVIEW.FMT, LIST; dot-stuffing both ways — codec (`rabbithole-legacy-nntp`, 51 tests) + a live gateway wired into burrow (`apps/server/src/nntp.rs`, config-gated `nntp_enabled`/`nntp_addr`, AUTHINFO→AuthService, POST→BoardService; e2e-tested). NEWNEWS/IHAVE peering deferred to the federation slice
- [x] AUTHINFO USER/PASS on TLS only (563/STARTTLS) — `legacy-nntp` gains `Command::StartTls` + 382 `ContinueTls`; `net::TlsIdentity::server_config()` reuses burrow's persistent cert (fingerprint-pinned, same identity as QUIC/WS); reader + peer-feed session loops made stream-generic with implicit-TLS listeners (`nntp_tls_enabled` :563, `nntp_feed_tls_enabled` :1563, both default off) AND STARTTLS on the plaintext ports (382 → handshake → RFC 4642 state reset; 502 when already secure); `nntp_auth_require_tls` (default TRUE, live) refuses plaintext AUTHINFO with 483 via `requires_secure_transport()`. New dep: tokio-rustls (ring-only). 5 e2e tests (pinned rustls client incl. STARTTLS upgrade + live knob flip). Self-signed identity only; ACME is future web work
- [x] Group↔board mapping (identity slug↔group); per-group monotonic article numbers; permanent Message-IDs (`<hex(blake3 event id)@origin>`); References threading; overview rendered from post metadata — in `apps/server/src/nntp.rs`
- [x] Peering: IHAVE/NEWNEWS with external peers; Message-ID dedupe via shared subsystem — codec (`legacy-nntp::transit` Exchange machine, `wildmat`, `datetime`, `listing`; 96 tests) **wired as a peer-feed service**: `apps/server/src/nntp_feed.rs` behind `nntp_feed_enabled` (default off; `nntp_feed_addr` :1120, `nntp_feed_peers` TOML allowlist — empty refuses all): AUTHINFO required before any transit verb (480 otherwise, TAKETHIS body still consumed), MODE STREAM/CHECK/TAKETHIS/IHAVE drive the codec Exchange, dedupe via the shared `SeenKey::MessageId` window (recorded on settle, both offered + header ids; native `<hex(event)@origin>` ids resolving to stored posts refused independently), articles validated to a known group and posted via BoardService with a gateway author seed (437/439 on malformed/unknown), NEWNEWS w/ wildmat+datetime for authed peers; reuses the reader's Message-ID/article parsing (`pub(crate)`). 3 e2e + 2 unit tests. Deferred: outbound feeding (receive-only + NEWNEWS pull), TLS on the feed port

**FidoNet**
- [x] PKT codec: type-2+ w/ type-2 fallback (capability word), packed messages, golden-file round-trip tests — `rabbithole-legacy-ftn` (bounds-checked LE reader, 5D addresses, CP437; 31 tests incl. 2000-iter fuzz)
- [x] Kludges: INTL/FMPT/TOPT/MSGID/REPLY/PID/TID; AREA:; Origin; SEEN-BY + PATH maintenance — `rabbithole-legacy-ftn::kludge` (canonical re-serialization, raw-CP437 text). Tosser/scanner/binkp/AreaFix/nodelist below are the service layer
- [x] Tosser + scanner services; ARCmail bundles (day-coded names + collision handling); BSO outbound — `rabbithole-legacy-ftn`: `tosser` (`Tosser::toss` classifies echomail vs netmail, cross-packet MSGID dedupe → `TossedBatch`, resolves 5D addrs from INTL/FMPT/TOPT, expands 2D SEEN-BY/PATH); `scanner` (group outbound into packets + pure BSO naming: lowercase-hex `NNNNnnnn`, `.?ut`/`.?lo` flavor matrix, cross-zone `outbound.NNN`, point paths); `arcmail` (`<hexdiff>.<weekday><seq>` with 0-9/a-z collision handling). Pure/sans-IO; the TCP mailer (binkp) service is the wiring slice
- [x] binkp mailer (FTS-1026, port 24554) — codec + FSM + service landed: `rabbithole-legacy-binkp` (2-byte block framing, M_NUL..M_SKIP, CRAM-MD5 auth verified against RFC 2202 vectors, sans-IO originating+answering session state machine; 42 tests); wired in `apps/server/src/ftn.rs` — tokio TCP listener (24554) drives the answering FSM to receive PKT/bundles into an inbound spool, `poll_uplink`/`run_originating` flush BSO outbound to a configured uplink; `ftn_*` config toggles, off by default. Crash-recovery resume + ARCmail zip decompression deferred
- [x] AreaFix (netmail commands: +/−/％LIST/％QUERY) — `rabbithole-legacy-ftn::areafix`: `parse` (password / `+`/`-` / bare-toggle / `%LIST`/`%QUERY`/`%HELP`, skips kludge/SEEN-BY/tearline) + `process` against `AreaFixConfig` producing the reply body
- [x] Nodelist + NODEDIFF parsing (CRC-16); echomail↔boards; netmail↔DM gateway; CP437 lossless round-trip — `rabbithole-legacy-ftn::nodelist` (parse, `apply_nodediff`, `crc16`/`verify_nodelist`) + CP437 via `::cp437`; gateways wired in `apps/server/src/ftn.rs`: inbound packets run the `Tosser`, echomail posts into the mapped board (AREA→slug) and netmail delivers as a DM (synthetic sender id 0), MSGID dedupe via `TossedBatch`, RBAC BOARD_POST/DM_SEND checked; outbound scanner stages local echomail posts (loop-guarded by `@{origin}` authorship) into BSO. 5 e2e tests

**RSS/Atom (inbound web syndication)**
- [x] Feed parsing: lenient hand-rolled XML pull tokenizer, RSS 2.0 + Atom 1.0 → `Feed`/`FeedItem`, manual RFC 2822 + RFC 3339 date parsing, HTML-to-text (tag strip + entity decode + char-boundary cap), blake3 dedup ids — `rabbithole-legacy-syndication` (blake3-only dep, 71 tests)
- [x] Feed → board ingestion layer (pure): `mapping` (`to_post_drafts(&Feed, &BoardMapping) -> Vec<PostDraft>` — subject/body/author fallback chains, canonical source link, reuses the blake3 dedup id), `seen` (`partition_fresh` + `SeenSet`, order-preserving, intra-batch repeat handling), `poll` (clockless conditional-GET state machine: `on_response(status, etag, last_modified, ttl, now)` covering 304/2xx/error with capped exponential backoff, TTL/`sy:updatePeriod` as minimum interval, saturating arithmetic). **Fetcher wired into burrow**: `apps/server/src/syndication.rs` — background service behind `syndication_enabled` (default off; `syndication_feeds` url→board map TOML-only, `syndication_poll_secs`), minimal HTTP/1.1 GET over tokio with sans-IO rustls + webpki-roots for https, conditional GET (If-None-Match/If-Modified-Since from `PollState`), 3-hop redirects, 1 MiB cap, timeouts; fresh items dedupe via `SeenKey::Syndication` and post via BoardService with a gateway author seed; 3 e2e tests (post-once → 304 → no dupes across restart). Feed-declared TTL wiring + IPv6 hosts + gzip are follow-ups.

**QWK/QWKE**
- [~] Packer: MESSAGES.DAT 128-byte blocks (0xE3 EOL, conf# @124–125), CONTROL.DAT, per-conf NDX with **MBF float** encode, DOOR.ID (QWKE advertised), bulletins — codec landed: `rabbithole-legacy-qwk` (MBF float verified against known vectors, Latin-1 fields; 40 tests incl. fuzz). **ZIP bundling landed**: `legacy-qwk::zip` (dependency-free bitwise CRC-32 + STORE-method `zip_store` — deterministic, fixed 0 mod-time so identical packets are byte-identical; CRC vs known vectors + a self-round-trip reader test) + `QwkPacket::to_zip()`
- [x] QWKE long To/From/Subject kludges (both directions) — `rabbithole-legacy-qwk::qwke`
- [~] REP ingest: validate, dedupe, post as signed events — `rabbithole-legacy-qwk::reply`: `ReplyPacket::parse/encode` over `<BBSID>.MSG` → `Vec<ReplyMessage>` (conference from the to-reader slot, reusing the MESSAGES.DAT record codec); `validate()` surfaces per-record `ReplyProblem` (conf out-of-range / empty body / malformed header); `content_hash` (blake3 over semantic fields) + `dedupe()` catches re-uploads and within-batch repeats. Posting the accepted set as signed board events is the server-wiring slice
- [x] Delivery: CLI/web export, telnet surface, scheduled per-user packets; read pointers shared with offline mode — codec (`build_packet`/`QwkPacket::members()`) **wired into burrow**: `apps/server/src/qwk.rs` behind `qwk_enabled` (default off, live; spool under `qwk_spool_dir`): conferences = postable boards sorted by slug numbered 1..=255 with the map in CONTROL.DAT; messages since the shared W3 `read_marks` pointers (200/conf + 1000/packet caps, oldest-first), pointers advance only over what was packed and only after the spool write (telnet refuses pre-build so unfetchable packets never eat mail); telnet `[M]` menu writes members + mints `files_http_base` links; ctl `qwk-build`/`qwk-ingest` (audited). REP ingest validates conferences, posts with the USER's author seed (BOARD_POST per board, threading via the NNTP-shared numbering), dedupes on blake3 content_hash under `SeenKey::QwkReply` recorded only after successful post. 4 e2e + 5 unit tests. **Real `.QWK` ZIP now written**: `build_for` writes a `<BBSID>.QWK` STORE ZIP (`packet.to_zip()`) alongside the raw members, surfaced on `QwkBuild.packet_path` + the ctl `qwk-build` reply (`packet`), with an e2e asserting the envelope + that each member's bytes are stored uncompressed inside. Deferred: HTTP route serving the `.QWK` directly, zmodem upload path, durable cross-restart reply dedupe; scheduling per-user packets
- [~] Syndication admin UI: per-board network mappings, feed monitor, dupe stats — `ui-web::syndication_admin` (pure panel model, 858 lines): gateway matrix (NNTP reader/feed/TLS, FTN, QWK, syndication — enabled state, port, live-vs-restart badges mirroring documented server semantics and overridden by real `ConfigApplied.applied_live` answers), read-only feeds table (`syndication_feeds` is TOML-only — `ConfigGet` NotFound folds to an honest "edit burrow.toml + restart" hint; total defensive TOML-fragment parser for dev/mock), bounded poll-interval editor; `SyndicationPanel` in the Admin view. 22 new tests (186 crate) + wasm green. **Live-stats server slice landed** (the documented seam): `apps/server/src/stats.rs` `GatewayStats` (in-memory counters, per-feed `last_poll`/status/items_seen/posted/dupes + per-gateway named counters bumped at existing success points — `nntp.sessions`/`.posts`, `nntp_feed.accepted`, `ftn.echomail_posts`, `qwk.packets_built`/`.replies_ingested`, `hotline.logins`, `radio.sources_connected`, `telnet.logins`), exposed over ADMIN 45/46 `GatewayStatsRequest`→`GatewayStatsReply` (CONFIG_ADMIN-gated, string-keyed counters so the set grows without proto bumps) + `ctl gateway-stats` (JSON); 2 unit + 2 e2e + proto roundtrip. Deferred: wiring the SPA panel to render the new reply; server-side read-only feeds exposure

## Wave 11 — Radio
*Depends on: W1, W4 (W8 for UI polish)*

- [x] Station/mount model (multiple stations, per-server toggle) — `rabbithole-audio` (PCM frames, mixer, `Station` fan-out, jitter buffer, VU meter; 21 tests) + `rabbithole-radio::StationRegistry` (create/remove/list, per-station enable toggle, listener accounting; 23 tests)
- [x] Playlist engine: rotation (Sequential/seeded-Shuffle/RepeatOne), **vote queue** + requests, `StationController` tying playlist→audio Station with now-playing/metadata — `rabbithole-radio`. Library-from-file-areas source wiring is the server slice **Requests reach people (0.261.0)**: the vote queue was never on the wire. `RadioRequestsRequest`/`RadioRequests` (the queue in the order it will play; whether the station takes requests and whether a DJ has the air), `RadioRequest`/`RadioRequestVote` (asking for a waiting song joins it), `RadioOfferRequest`/`RadioOffer` (RADIO 7-12): what can be asked for is looked through with a search, a hundred at a time with how many more, and titles and comments are cut to 200 characters, so a library of any size stays inside a frame. Only what a station can play is offered or taken: not audio it cannot send or FLAC in another form than its mount's (both known at install), not a track the pump found it could not play (until it plays), not what is playing now, and nothing a moderator is holding back; held requests do not use up anybody's three and take no votes. Neither driver plays or announces a held file any more, which 0.260.0 missed. An account that may talk on the `radio` resource, three waiting each and fifty a station, rate-limited like a post; each refusal has its own code and words (the cap is `TooLarge`, the posting budget `RateLimited`, the song playing now `AlreadyExists`). A DJ taking the air no longer spends the top request on nobody: the rotation moves on as they leave, so what is announced then is what plays. Passing over requests that cannot play no longer counts as a whole rotation of nothing. The timer driver and the pump now keep one clock, so a station handed from its pump to the timer still reaches the end of a song. A track left out is logged once per reason, not every time the rotation passes it. Radio pane: "Asked for" under the player, per burrow and asked of the burrow whose stations are shown; the queue with Me too, a search to ask from, a refusal said beside the station that said it, until the song changes or the person comes back, and never for a look the pane took itself; the search closes when the view moves to a burrow where the person cannot ask. A reply sink that sends again panicked inside the socket's borrow and silently killed every later send, so the pane's sends go through `app::defer`. Still open: a station's rotation is read at startup, so a file added to its area is not playable or requestable until a restart; others' requests are seen on the next song or a revisit, not pushed.
- [x] DJ live source (Icecast SOURCE/PUT + Basic auth) — works with butt/ices — dedicated source-ingest listener (`radio_source_enabled`/`radio_source_addr` :8001, `radio_source_user`/`radio_source_password` config creds, off by default): `legacy-icecast::parse_source_request` auth → OK2/401/403, mount claim into the existing per-mount byte fan-out, DJ pre-empts the playlist program (`go_live`) and rotation resumes on disconnect (graceful shutdown); now-playing from `ice-*` headers. Library-from-file-areas playlist source (`radio_library_areas` area→mount map via FileService → `tracks_from_nodes` → `install_program` + 1s playlist driver). Bytes→PCM decode remains a follow-up; **mid-stream metadata codec landed**: `legacy-icecast::admin` (`parse_metadata_update` — `/admin/metadata` + `admin.cgi` updinfo, total percent-decoding, query-`pass`/Basic-auth with header-wins, Icecast `<iceresponse>` XML replies incl. 401 challenge) + `::metaread` (`MetaintReader` incremental client-side de-interleaver: chunk sizes 1..=17, empty/NUL blocks, malformed-block counter without desync) + `StreamTitle` encode/parse helpers; 86 unit + 3 doctests. **updinfo wired**: the source-ingest listener sniffs `parse_metadata_update` before source ingest — creds vs `radio_source_user/password` (empty-password fail-safe), mount from `mount=` or the sole live mount (admin.cgi), "Artist - Title" split, now-playing + presence republished (pure-DJ mounts too), XML `<return>` / 401 replies. 6 e2e + 3 unit tests
- [~] Encode pipelines: Opus/Ogg primary + MP3 legacy mount — **Ogg plays natively (0.241.0)**: `rabbithole-radio::ogg` walks pages and reads what a stream carries (Opus at 48 kHz, Vorbis at its own rate) without decoding it, `radio::Sound` picks how a track is paced and what the mount says it is (`audio/mpeg` / `audio/ogg`), and a library of Opus or Vorbis files streams as an Ogg station instead of being skipped. A station sends one kind of sound — a listener's decoder is never handed another part-way — and a track of the other kind is left out with a warning naming it. **The operator can see it (0.242.0)**: `RadioStatusRequest`/`RadioStatus` (RADIO 5/6, `CONFIG_ADMIN` only) answers with every installed station — silent ones included — its file area, what it is sending, what is on, listeners, rotation size, and the last twenty tracks it had to leave out with the reason for each. **The console shows it (0.243.0)**: the Radio section gained a Stations panel — each station's sentence (what is on, from how many tracks, as MP3 or Ogg), where its music comes from, who is listening, a DJ-live pill, and an expandable list of the tracks it could not play with the reason for each. `AdminCommand::ListStations` rides the console's own seam. **A mount of each kind (0.244.0)**: a library holding both is a station of each — the MP3 files on the bare mount as always, the Ogg files beside them at `<mount>.ogg`, playing at the same time, so nothing is left out for being the wrong kind and a listener picks which to tune in to. A library of one kind is one mount, exactly as before, and a station's mount goes up as what its track names say rather than guessing and being remade under whoever is already listening. **FLAC too (0.245.0)**: `rabbithole-radio::flac` walks a FLAC file without decoding it — STREAMINFO for what the stream is, then frames, each found by scanning for the next one and checking both checksums the encoder wrote (CRC-8 over the header, CRC-16 over the frame), because a FLAC frame header does not say how long its frame is. A library of FLAC files streams as `audio/flac`; a mixed library gets a mount per kind. Checked against files the reference encoder wrote: 8, 16, 24 and 32 bits, 8 to 96 kHz, block sizes 256 to 4096, padding, tags, and an ID3 tag at either end. **What review found (0.246.0)**: seven things, each reproduced against the reference encoder before it was fixed. A station's mount goes up as the kind its library was sorted by rather than a vote over file names that the files it cannot play could win, so a FLAC station is never announced as MP3 and then remade under whoever is listening; a mount an operator names for a kind — `jukebox.flac` — is that kind's mount, and the others hang off the base of the name; libraries are handed their mounts in name order, so two of them cannot take a name from each other differently at every start. In the walker: a file whose STREAMINFO understates its longest frame (what `ffmpeg -c copy` writes when it cuts two files together) no longer has the whole rest of it taken for one frame, because the CRC-16 of a run of whole frames is the CRC of the last one and so agrees at every boundary; an APEv2 or Lyrics3 tag on the end no longer costs the track its last frame; a megabyte of zero padding is no longer sent as if it were the last of the music; a frame that says it plays at 0 Hz is not a frame; and headers with no frame behind them are not a track. A track that walks to no audio is left out with a reason and counts toward the station's backoff, instead of racing the rotation through at disk speed, and no single send is larger than a megabyte whatever the timing arithmetic says. **One stream, not a file after a file (0.247.0)**: a FLAC mount sends its own `fLaC` header once and gives it to every listener as they connect, then sends each track's frames renumbered to carry on from the one before. A second header in the middle of a stream is what made libFLAC stop dead at the end of the first song — verified with the reference decoder, before and after, against a burrow's own mount. Because that one header fixes the rate, the channels and the depth for the whole night, a station sends one form of FLAC, and a track of another form is left out with a line saying which form it is not. **What review found in that (0.248.0)**: three more, two of them found by reproducing them with ffmpeg. A sample number past 2^31 is written in the seven-byte coded shape, which libFLAC reads and libavcodec refuses outright — so a station that had been up about thirteen hours went silent for every player built on ffmpeg (mpv, ffplay, Kodi, Chrome) and stayed silent; the count now stops short of that shape and starts again between songs, where a jump backwards costs nothing. A station's form is settled when it is installed, by what most of its library is, read from the front of each file: one voice memo at the top of a folder of albums used to pin the mount to 8 kHz mono and leave every album out for good. And a track is only cut into sends once the station knows it will send it, so a shelf of the wrong thing is read rather than read and rewritten, and the clock starts again when a run of them has left the station further behind than a listener's buffer is deep. **And a tag in front of the music (0.249.0)**: a library takes a kilobyte look at each file to see what form it is, and a tag with a picture in it is longer than that — the look now goes back for the part that matters, using what the tag says about its own length rather than what fits in the first look. `mp3::id3v2_says` is that number; `id3v2_len` is still it clamped to what a walker is holding. **The console pane, actually opened (0.250.0)**: the Stations panel was signed into and looked at for the first time, against a burrow with a real library, and it was empty — every station missing. A tagged admin reply is routed by its tag, and the router held a hand-written list of prefixes that `*stations` was never added to, so the answer landed in the settings model where nothing reads it. The rule is now the one that was always true: a tag that starts with `*` belongs to whoever asked for it, and the two the settings model asks with are named as the exceptions. Its panel had no styles of its own either — the class names were invented and never written — so the station name, what it is playing and where its music comes from ran together on one line. It is a row in the console's own idiom now, at both widths. **What review found in that (0.251.0)**: the library's vote could be won by files that can never play. A download that stopped after its metadata still says what it was going to be, and three of those outvoted two whole albums — the station was installed as a form nothing in it could play, the pump seeded itself from that, and it stayed silent forever over a shelf of music (the tie-break made it worse: it favoured the highest rate, which is the shape of a stalled hi-res download). Only a file that shows audio where its headers end votes now, and one that merely says so votes when nothing better does; a tie goes to whichever came first in the area. The seed is a guess either way, so a rotation that leaves out every track drops it and takes the next thing that can play. Also: a sample count that wrapped inside a very long track no longer hands the next track numbers it just wrote, the pacing clock starts again after the thirty-second backoff instead of firing half a minute of audio in one breath, and a tag between 983 bytes and a kilobyte no longer costs a file its vote. **The audit log records what it says it records (0.252.0)**: the Moderation pane promises "every change an operator or moderator made" and missed half of them. A board made, a file area made or renamed or taken away, a folder, a drop box, a shortcut, a hash refused or allowed again, a report acted on, something held back or let through, a moderator removing somebody else's post or file, somebody kicked from a room: none of it was written down, though the console already had words waiting for most of them. All of it is recorded now, and only when it is somebody else's business — a person tidying up their own upload or taking down their own post is not an operator action. Every one of the 57 things a burrow records also reads as a sentence rather than as its own name, held there by a test. **And the same log, read out loud (0.253.0)**: refusing a hash wrote *two* lines, because the moderation service in `server-core` had been recording reports, quarantines and the deny list all along and the new entries were added beside them. The duplicates are gone and the console words the names a burrow actually writes (`deny-hash-add`, `report-claim`, `report-create`, `report-dismiss`), which it never did — the words it had were for names nothing wrote. A 64-character id inside a line is shortened the way the rest of the console shortens one, wherever it sits. And the status line above the theme editor, which is the console's one shared "what just happened", no longer greets an operator with the last thing they did in another pane. **Three people who were never there (0.254.0)**: on a live burrow the lobby showed `rabbit`, `alice` and `bob` — the demo's roster — and the Directory then said everyone was offline, yourself included. The console's Moderation pane refreshes the roster when it opens, and the refresher it called only ever read the seeded demo client; so did the live presence path, every time somebody left. It asks the burrow now when the session is live, which is the same reply the pane's own session list comes from. Found by signing in and looking at the lobby. **A blank window mends itself (0.255.0)**: mid-session the web app stopped rendering entirely — the browser had refused the bundle, because what the service worker had kept no longer matched the integrity hash in the document that asked for it, and cache-first meant the same copy came back at every reload. Nothing on screen said so; the only cure was knowing to clear site data. The shell watches for it now (a failed module import, or an app that has simply not started long after it should have), clears the worker and its caches once per visit, and reloads. Proved by poisoning the cache with a bad bundle: the window came back on its own. The service worker's comment claimed this could not happen, and no longer does. **The art tab shows the burrow's art (0.256.0)**: it used to render one built-in sample and nothing else — a tab in the navigation of every burrow, showing the same picture on all of them. It is a view of a file library now: pick an area, and everything in it this pipeline can draw is there to look at without downloading it first. `art::looks_like_art` is the rule (`.ans`, `.asc`, `.nfo`, `.diz`, and a recorded ANSI type), and a burrow with no art of its own still shows the sample rather than a blank rectangle. The bytes come back over the same path a download does — `DownloadedFile` carries the node id now, and the transport hands them to whoever asked instead of always saving them. **The Wishing Well, at last in the app (0.257.0)**: the request board has been in the protocol since Wave 3.2 and in the `rabbit` CLI, and the web app had an icon for it and a route in the command palette's tests — and no pane. There is one now: what people have asked this burrow for, filtered by whether anybody has taken it on, with a wish of your own, a vote to add to somebody else's, taking one on and saying what came of it. `wish.rs` holds the words and the reading order (nothing taken on first, most wished-for at the top, what is over at the bottom) and is tested; the demo seeds three so the pane is a well rather than a hole. **Rooms, in the app (0.258.0)**: a burrow has had rooms since Wave 3 — make one, go in, invite, topics, kicks and mutes, all answered by the server — and the web app knew only the lobby, sending every line there and keeping one scrollback for all of it. The lobby pane lists the burrow's rooms now, makes one, goes in, comes out, and what is said stays where it was said: `ChatLine` carries its room, the reducer keeps it, and the pane shows one room at a time. The compose box names the room it will send to. The room list is asked for when the session signs in, not only when the pane opens, because a pane can be opened before the socket is up. **And the rest of a room (0.259.0)**: a room made private from the app, its topic set by whoever made it, and somebody asked in by handle. What a burrow refuses is said where a person is looking — an invitation to somebody who is not here used to be answered with nothing at all, because a CHAT refusal reached no part of the app; it now lands in the scrollback as a line from the burrow, in words rather than a code. Still to do: transcoding, so one rotation can hold both kinds and a station can offer an Opus mount beside an MP3 one
- [x] Delivery: ICY mounts w/ exact icy-metaint math — codec (`rabbithole-legacy-icecast`, 43 tests) + a live listener wired into burrow (`apps/server/src/radio.rs`, config-gated `radio_enabled`/`radio_addr`): SOURCE/PUT DJ auth via AuthService+`Caps::BROADCAST`, per-mount broadcast fan-out, listeners get metaint-spliced `StreamTitle`, lagging listeners dropped; e2e-tested. Native QUIC uni-stream + HTTP/Ogg transports are later refinements
- [~] Listener counts in presence; now-playing surfaced (presence line, TUI status, telnet, web) — server side landed: `PresenceRegistry` gains `RadioStatus` + `set/clear_radio_now_playing`, publishing `ServerEvent::RadioNowPlaying` on the bus. **TUI landed**: `apps/tui/src/radio.rs` — status-bar `♪` segment + Ctrl-N station panel (live-DJ-wins reducer, render-to-lines, truncation; 9 tests), fed by the `[radio]` ServerNotice bridge — **server emission wired**: `push_for_event` projects `ServerEvent::RadioNowPlaying` to the bridge-format ServerNotice (live flag from `presence.radio_status`, never recorded to offline replay) and station teardown publishes the `off` notice; format round-trip tested against the TUI parser spec. **RADIO proto family now landed** (retiring the bridge): proto `Family(9)` gains `RadioNowPlaying`/`RadioOff` push messages (registry `EXPECTED` 177→179, golden re-blessed); `push_for_event` emits typed frames (new `ServerEvent::RadioOff` for teardown) instead of the `[radio]` ServerNotice; the TUI (`RadioNowPlaying` → `RadioStatus` via `From`) and web (`frame_to_notice_route` decodes RADIO frames) clients decode the family and the string bridge + all its parsers/round-trip tests are deleted on all three sides; docs (framing/versioning/README) updated. **Telnet now-playing surfacing landed**: the telnet MAIN MENU renders an ASCII-only `Now playing on <station>: <title> - <artist> (DJ …) [N listening]` banner (pure `radio_now_playing_line`, live-DJ-featured, safe across CP437/UTF-8 TTYPEs, from `presence.radio_now_playing()`; 3 unit tests, telnet e2e green). Now surfaced on every rich + legacy client.
- [~] Client players: GUI/web, TUI handoff, per-user enable + volume/ducking settings — **web player landed**: `ui-web::radio` (total `[radio]`-bridge notice parser + `RadioState` reducer, `RadioPrefs` {enabled, volume, muted, station, base} validated + localStorage-persisted, `stream_url` mount-join w/ http/https allowlist) + `player.rs` (wasm `HtmlAudioElement`: src-change-only restarts, play/pause/volume/mute `sync`) + Radio view (LIVE/auto badges, listeners, delivery-address input) + StatusBar segment; ServerNotice gained a wire mapping (`NoticeRoute` Radio/Chat split mirroring the TUI) with operator notices landing in scrollback. 20 new tests (156 crate) + wasm green. **TUI handoff landed**: `apps/tui::handoff` (base/URL rules mirroring ui-web verbatim, `$RABBIT_PLAYER` detached spawn w/ reaper thread + status-line errors, Radio view w/ selectable stations, copyable URL; **RH-101**: validated delivery bases saved per burrow in the shared atomic preference file; session `--radio-base` overrides and `$RABBIT_PLAYER` remain unsaved, with visible save-failure recovery). The `[radio]` bridge both clients decoded is now retired in favour of the typed RADIO family (see the listener-counts line). **RH-100**: observed play-promise and stream-error states, inline Start listening/Retry playback controls, and stale-attempt protection; passive volume/mute/listing updates do not retry blocked playback. Remaining: ducking. **The burrow says where to tune in (0.210.0):** the client no longer asks anyone for “your server’s Icecast delivery address”. New RADIO messages `RadioStationsRequest` (3) → `RadioStations` (4) carry `stream_base` (new live config key `radio_public_base`, for a TLS proxy) or the listener’s bound `port` (the client derives `http://<host it dialled>:<port>`), plus per station its name, whether audio can be had (`streaming`), a cover blob id and its last ten tracks. Server: a ten-deep per-station history that moves only on a change of track (`Stations::note_now_playing`, one publish door), covers found the way music folders do it (`covers_from_nodes`: an image named like the track, else `cover`/`folder`/`front`/`album`), `burrow --radio`, and `scripts/stack.sh` turns it on. A DJ mount on the delivery port used to go live in silence; it now announces on and off the air. Client: `RadioState::stream_url`/`Tuning`, covers over `BlobGet`, a generated record sleeve per track when there is no art, the player redrawn around now-playing + Recently played, and a stated reason whenever Listen is off (demo, radio off, a rotation with no encoder, not heard yet). **A library rotation is a radio station, not a list of titles (0.214.0):** with the stream listener on, each `radio_library_areas` station gets a pump (`radio::spawn_program_pump`) that plays its rotation out loud. `rabbithole_radio::mp3` walks a file frame by frame (four-byte MPEG headers only: no decode, no dependency; ID3v2 skipped by its declared size, junk resynced over, and a candidate frame believed only if the file’s other frames agree with it on layer and sample rate — a test caught `FF FF 67 61`, the start of “\xFF\xFFgarbage”, parsing as a legal Layer I header). The pump sends whole, contiguous frames in quarter-second batches, two seconds ahead of one station-wide clock, skips the sends when nobody is listening, and **advances when a track’s audio ends** rather than on the nominal three minutes. A rotation’s mount is `program_owned`: a DJ on either source surface takes it over instead of getting a 403, the pump notices within a batch and stands down, and the rotation picks itself back up with the next track when the DJ leaves. Non-MP3 tracks are skipped by the pump (it cannot pace what it cannot walk); a rotation of nothing else stays now-playing-only and the listing says `streaming = false`. End to end in `e2e_w114`.

## Wave 12 — Mobile & distribution
*Depends on: W8 (W11 for background audio)*

- [ ] Tauri iOS/iPadOS + Android builds; mobile plugin glue: notifications, background audio + audio session, share sheet
- [~] Transport resilience on mobile (QUIC connection migration, WS fallback) — **QUIC migration landed**: `net::Connection` gains additive (default-bodied, non-breaking) `migrate()` + `local_addr()`; the QUIC client now RETAINS its `quinn::Endpoint` so `migrate()` calls `Endpoint::rebind` with a fresh wildcard `UdpSocket` — the live connection moves to a new local socket/port (WiFi↔cellular) WITHOUT re-handshaking (connection IDs carry it). New `NetError::Unsupported(&str)` is the documented "fall back to reconnect+auth_resume" signal returned by WS + server-side QUIC. `tests/quic_migration.rs` proves it: post-migrate the local addr changed, the remote addr didn't, a 2nd request round-tripped on the SAME accepted connection (server never re-accepted), and WS migrate reports Unsupported. 8 net tests. **Auto-reconnect helper landed**: `Client` now remembers its dial params + the resumable session token (captured from `auth_password`/`auth_resume`; guests get an empty token → not resumable) and gained `reconnect()` (re-dial + Hello + `AuthResume` with the never-rewound replay cursor, preserving buffered-but-unread pushes ahead of replayed ones), `is_resumable()`, and `request_resilient()` (retries once on a transient network drop — documented at-least-once, for idempotent reads). A pure `is_transient(&ClientError)` classifier (host-tested) gates the retry so refusals/decode errors never loop. 1 host + 2 e2e (`e2e_w12_resilience`: a password session reconnects → resumed=true, same identity, cursor doesn't rewind, who+chat work after; a guest session is refused a resume). QUIC `migrate()` stays the proactive WiFi↔cellular path; `reconnect()` is the after-a-drop recovery path
- [ ] App Store (TestFlight) + Play (.aab) packaging, signing, privacy manifests, entitlements
- [x] `dist` release automation (CLI/TUI/server): archives, installers, Homebrew — `.github/workflows/release.yml` (tag-triggered cross-platform binary archives + checksums + GitHub Release) and `scripts/release.sh` landed. **Distro packages + one-command stack landed (0.191.0)**: `packaging/nfpm.yaml` describes the file list once and `scripts/package.sh` renders it (nfpm does *not* expand `${VAR}` inside `contents[].src`, so the script sed-renders to a temp config) into **deb + apk + rpm** for the host arch — verified end to end (`rabbithole_0.191.0_arm64.deb`, 13.6 MB, correct control metadata + contents). Installs all four binaries to `/usr/bin`, both systemd units as `config|noreplace`, and `/var/lib/burrow` 0750 burrow:burrow; maintainer scripts create the `burrow` user and `daemon-reload` but deliberately never enable/start a service or delete data on removal. `contrib/looking-glass.service` mirrors the burrow unit's hardening. `packaging/homebrew/rabbithole.rb` consumes the archives `release.yml` already publishes (no second artifact) with a `service do` block and a test that asserts the *version* matches, not merely that the binary exists. `release.yml` gained an `aarch64-unknown-linux-gnu` row and a package-building step. `justfile` + `scripts/stack.sh` are the one command for the server side (`just up` = burrow with the SPA + tracker, one Ctrl-C stops both); the SPA failing to build warns instead of taking the stack down, and `WEB_ROOT` is absolute because burrow resolves a *relative* `--web-root` under `--data-dir`. `Dockerfile`/`docker-compose.yml` now build the SPA and run the tracker alongside the burrow. **Also fixed a real blocker**: `crates/ui-web/index.html` needed `data-wasm-opt-params` post-MVP feature flags — without them `trunk build --release` failed outright, so release SPA builds were impossible
- [x] Docker images (multi-stage → slim) + docker-compose; systemd unit; install docs — `Dockerfile` + `.dockerignore` + `docker-compose.yml` + hardened `contrib/burrow.service` + `docs/deployment.md` (accurate to real bins/ports/env vars)
- [~] Versioned protocol docs published (docs site) — the spec itself is current in-repo (see W-Continuous lockstep line); publishing to a docs site is the remaining step

## Wave 13 — Hardening & 1.0
*Depends on: all*

- [~] E2EE DMs + private rooms: X25519 + Double Ratchet (1:1) + **Sender Keys (groups)**, ChaCha20-Poly1305, sealed sender — crypto core complete: `rabbithole-e2ee` (Double Ratchet, X3DH-lite, sealed sender, Ed25519-signed sender-key groups; 43 tests). **1:1 DM E2EE wired (opt-in, additive, server-opaque)**: prekey bundles (DM family types 10–12 `KeyBundlePublish`/`Request`/`KeyBundle`; store migration 0010 `e2ee_bundles`+`e2ee_one_time_prekeys`, public keys only, atomic `DELETE…RETURNING` OTP consume w/ no-OTP fallback; `server-core::keybundle`) + `DmSend`/`DmMessage` gain `#[serde(default)] encrypted: Option<EncryptedPayload>` (ratchet Header+ChaCha20-Poly1305 ciphertext; first-msg `PrekeyPrologue` carries initiator identity+ephemeral pubs; Ed25519 signed-prekey verified before session start) — server stores the blob opaquely, forces `text=""`, never decodes; `crates/core::e2ee` X3DH init + encrypt/decrypt helpers alongside the intact plaintext API. e2e proves the stored row is ciphertext-only + ratchet advance + OTP-exhaustion fallback; proto registry 174→177 re-blessed. Deferred: group/room Sender-Keys wiring, key-backup UX, multi-device
- [~] Moderation suite: report queues, quarantine-for-review, shared blocklists + blake3 hash-deny lists, moderation audit UI — server side landed: `store-server` migration 0008 + `repo7` (reports w/ state-machine guard, quarantine, deny_hashes; STRICT+indexed); `server-core::moderation` (`ModerationService` — file_report deduped by (reporter,subject) while open, claim/resolve/dismiss, quarantine + deny mirrors warmed at boot, every mutation audited); new `Caps::MODERATE` (1<<54, Moderator default — dedicated bit since the queue spans posts/DMs/files/users); ADMIN family types 30–40 + `handlers11`; moderators get a ModNotice push on new reports. Quarantine enforced on native RHP paths (thread list/fetch, file list/search, node get, inline+bulk download, folder manifest — files by blob hash so aliases covered); deny-hash at upload finalize (staging deleted), inline upload, DM attachments. 4 e2e tests. **Room mute + slow-mode landed** (the W2.2 deferrals): per-room mute list (timed w/ lazy expiry on the injected-clock convention, permanent when no duration; creator-or-CHAT_MODERATE gated, creators unmutable, lobby included) + per-room slow-mode (between-message minimum per member, moderators/creator exempt, 0=off, clamped to 3600s, distinct from the global `msg` budget) in `server-core::chat`; CHAT family types 23–27 (`RoomMute`/`RoomUnmute`/`RoomSlowMode` + member-scoped `RoomMuted`/`RoomSlowModeChanged` pushes) + new `ErrorCode::Muted` and `ErrorCode::SlowMode{retry_after_secs}` (retry-after rides the error code); enforced in the shared send path so native, Hotline (private ChatMsg refusal line — ChatSend is a notify), and telnet (parenthesized refusal line) all observe it; `room-mute`/`room-unmute`/`room-slow-mode` audited; 3 unit + 5 e2e tests (`e2e_w13_chatmod`). **Deny-list now reaches the blob path** (closing the noted gap): RHP `BlobPut` (rejects deny-listed content before storing, matching the blake3 id `put` would return) and `BlobGet` (never serves a denied hash) now consult `moderation.is_denied`; plus a local-operator `hash-deny`/`hash-allow`/`hash-deny-list` ctl surface over the same `ModerationService` list (1 e2e: deny→serve+ingest refused→list→allow→restored). **Quarantine is true on every surface (0.260.0)**: it was not. Hotline listed, described and served content held for review, and the one funnel every gateway reads a board through — netnews, QWK, the peer feed and the catalog a burrow federates — read held posts out with the rest; FidoNet scanned them into packets that cannot be recalled. All of them consult it now, and a burrow no longer advertises held or refused files to its peers. What is left: shared cross-server blocklists + the moderation audit UI; Hotline/telnet don't render the mute/slow-mode *change* pushes (refusals only)
- [~] Rate limiting everywhere (buckets per IP/account/endpoint-class); mCaptcha option; invite trees — **limiter landed**: `server-core::ratelimit` (hand-rolled token buckets, injectable clock, lazy buckets + expiry sweep, saturating math, `peek_with` so successes never spend auth budget; 8 tests) with six classes wired across every surface — `conn` 30/min/IP at all accept loops, `auth` 5/min/IP on failures (native, AUTHINFO, Hotline, telnet, radio SOURCE/updinfo), `msg` 10/s/account (ChatSend/DmSend), `post` 6/min/account (native, NNTP POST, Hotline news), `transfer` 10/min/account (native TransferOpen after the concurrency cap, Hotline DownloadFile), `legacy` 20/s/IP per command; 13 `ratelimit_*` config knobs (`ctl config set`, 0-rate disables), sparse once-per-key-per-minute audit; 4 e2e tests (flood→RateLimited while session survives, AUTHINFO hammer→481+close, conn drop, disabled→no limits). Deferred: finger conn gate (accept loop lives in the crate), binkp/S2S auth classes, Retry-After propagation; mCaptcha is a separate slice. **Invite trees landed**: `0012_invite_lineage.sql` adds `accounts.invited_by`; `register()` now atomically `reserve`s the invite (capturing `created_by`), `finalize`s `used_by` to the real redeemer (fixing the old `0` placeholder), and records the tree edge; `AuthService::invite_subtree` walks a bounded breadth-first downline (`AccountsRepo::invitees`), surfaced via ctl `invite-tree` — an operator can trace and act on a whole downline. 2 unit + 1 e2e. (Invite codes themselves landed in W2.)
- [~] Fuzzing coverage goals (all codecs); RUSTSEC audit gate in CI; security review checklist/pen-test pass — **audit gate landed**: `ci.yml` gains a `security audit` job (cargo-audit via install-action) + `.cargo/audit.toml` (one documented ignore: RUSTSEC-2023-0071 `rsa` Marvin — transitive via sqlx-mysql, never linked since we're SQLite-only, no upstream fix yet; 4 unmaintained/unsound warnings noted, non-failing). Codecs already carry fuzz-style totality sweeps in-tree; cargo-fuzz targets + the security checklist/pen-test are the remaining slices
- [~] Load harness (target: 10k concurrent sessions/server) + performance pass — `apps/loadgen` (`warren-stampede`): clap CLI over a library scenario driver reusing `rabbithole_core::Client` (WS; QUIC via fingerprint) — idle/chat/mixed scenarios, ramp rate, jittered send intervals, echo-matched RTT, bounded jittered reconnects, atomics + sorted-vec p50/p95/p99, 5s progress lines, `--json` report, Ctrl-C drain + `--max-errors` breaker (exit 2). CI smoke: real in-process burrow, 50 guest chat sessions / 5s WS — 0 errors, connect p95 1.2ms, RTT p95 47ms. The 10k run is a documented real-hardware target (README); the performance pass remains
- [~] Accessibility pass (web/GUI); i18n scaffolding — **web pass landed**: `ui-web::a11y` (checklist doc-module, id vocabulary, label-pairing helpers, wasm-gated route-change focus) + full markup sweep across all 11 views (landmarks + one focusable h1/view, fixed heading skips, label/for everywhere, `role=log` scrollbacks, `role=status`/`alert` live regions, real tables w/ scoped headers, aria-hidden glyphs w/ sr-only text, `aria-current`/`aria-pressed`, fieldsets, login is a real form so Enter submits); stylesheet gains `:focus-visible` on a new `--rh-focus` token (3:1-swept across all 6 pack×modes), skip link, `prefers-reduced-motion` blanket; theme editor warns on focus-pair contrast at 3:1. Fixed 2 live bugs (router-intercepted preview anchors, Enter focus loss on token commit). 15 new tests (201 crate) + wasm green. Remaining: axe-core/Playwright sweep (listed in a11y.rs), GUI/TUI pass, i18n scaffolding
- [x] Backups: snapshot + restore tested; migration guides — `apps/server/src/backup.rs` + `ctl backup`/`backup-verify` (audited) + offline `burrow restore <snapshot> --data-dir <dir>` subcommand: consistent point-in-time snapshot (DB via `VACUUM INTO` through the sqlx pool — read-transaction-consistent under WAL, no second driver; then identity/, approved_peers.json, federation catalog, blobs skipping `*.tmp` staging) with a `MANIFEST.json` (blake3+size per file, hashed off-runtime); verify re-hashes + `PRAGMA integrity_check`s the snapshot DB; restore refuses while the ctl socket answers, verifies first, moves the old dir aside (`.pre-restore-<ts>`, never deletes), copies only manifest-listed paths, preserves 0600 key perms. 3 e2e (full seed→backup→mutate→restore→reboot with identity intact + corruption caught + fresh-dir boot) + 3 unit tests. Operator flow (stop→restore→start) in module docs; migration guides remain a docs slice
- [~] 1.0: docs site complete, flagship sample server config, launch — **docs site scaffolded**: `docs/book.toml` (mdBook, `src=.` builds the existing docs tree in place, `create-missing=false` so a broken TOC link fails the build, mirrorward edit-links) + `docs/SUMMARY.md` (every current doc linked: 14 protocol pages incl. versioning + federation, 3 operator guides, 9 design/decision records — all resolve) + `docs/index.md` (honest 0.67.0 status landing page); `mdbook build docs`. Flagship sample config shipped (`examples/flagship-burrow.toml`). Remaining for 1.0: publish/host the built site, the launch checklist

## Wave 14 — Reticulum & off-grid mesh (post-1.0 / 1.1)
*Depends on: W3, W9, W13*

- [x] Spike: `reticulum-rs` maturity vs Python RNS gateway sidecar → decision — `docs/research/reticulum-decision.md`: **decision = keep the in-repo pure-Rust `rabbithole-reticulum` sans-IO crate as primary** (single static binary, injected clocks/RNG, 99 tests, `#![forbid(unsafe_code)]`), with a Python `RNS` sidecar as a **test-only reference/conformance peer** (not a production dep); community `reticulum-rs` declined for now (maturity unverifiable from the sandbox + added supply-chain surface). Acceptance gate before any live RNS deployment: reconcile the 7 SPEC-CHECK points + 2 module divergences against the reference peer
- [~] RNS interop foundation: identity (X25519+Ed25519), destination/name hashes, packet + announce codecs, ECDH+AEAD token — `rabbithole-reticulum` (spec-faithful hashes, never-panic decoders, documented cipher/field divergences). **Transport-layer model landed**: `link.rs` (LinkInitiator/Responder sans-IO FSMs — LinkRequest 64B, LinkProof 96B signed over link_id‖resp pubs, encrypted LRRTT, Pending→Handshake→Active→Closed w/ injected-clock deadlines, per-direction HKDF keys + counter nonces + replay rejection isolated in `LinkCipher` for one-place upstream reconciliation), `DestinationHash` newtype w/ pinned vectors, MTU-500 enforcement + `packet_hash` invariant-under-transit, `AnnounceCache` (TTL + min-interval verdicts, injected clock). 99 tests; 7 SPEC-CHECK points doc-flagged + test-pinned for the interop pass. The live adapter (Burrow as a Reticulum destination) drives this core
- [~] LXMF bridge: DMs ↔ LXMF (delay-tolerant, NomadNet-compatible); boards ↔ LXMF propagation nodes (shared dupe subsystem) — message layer landed: `rabbithole-reticulum::lxmf` (`LxmfMessage`/`SignedLxmf`, `hash()` = SHA-256(dest‖src‖packed payload), Ed25519 sign/verify, deterministic postcard packing via a sorted fields map, total `unpack`; 22 tests). Wiring DMs↔LXMF over the RNS transport + propagation-node board bridge (with the shared dupe subsystem) and reconciling the packing with upstream MessagePack are the transport slices; the LXMF stamp/PoW is deferred
- [~] Delay-tolerant Tunnels (S2S flood-fill) over RNS with bandwidth-aware batching — pure sans-IO model landed in `rabbithole-reticulum`: `tunnel` (`TunnelMessage` content-addressed id + total wire codec, `MessageStore` w/ TTL + id-dedup seen-set + lazy purge, injected clock; `MAX_TUNNEL_PAYLOAD=384` single-packet), `floodfill` (`FloodFill::plan_forward` = all peers but source + known holders, hop-increment/ttl-drop, loop-safe `ForwardLedger` w/ TTL), `batch` (`Batcher` packs same-peer messages into `DEFAULT_BATCH_BUDGET=441`-byte envelopes — HEADER_2 465 − 24 cipher reserve, compile-time-asserted to fit; priority+age ordering; per-peer all-integer milli-byte `TokenBucket` governor; partial flush on age). 58 new tests (157 crate); 5 SPEC-CHECK points + divergence #6 doc-flagged. The live RNS tunnel adapter/sidecar drives this core. Deferred: fragmentation (single-packet only today), IO/encryption/announce-driven peer discovery
- [~] rabbit links w/ RNS destination hashes; Looking Glass entries may advertise RNS destinations — link format landed: `RabbitLink` authority is now `LinkAuthority::{Host, Rns(DestinationHash)}` with the `rabbit://rns:<32-hex>/…` form (reserved case-insensitive prefix, total parse w/ exact error offsets) + repeatable `?via=rns:` dual-authority alternates (sorted+deduped canonical Display, unknown params/schemes tolerated for forward-compat), `*_rns` builders + `with_rns_alternate`/`rns_routes`, back-compat `host()`/`port()` accessors (all consumers still compile); 38 swarm tests (13 new; old vectors kept verbatim). Resolver semantics documented only (IP first, RNS alternates for delay-tolerant paths); tracker RNS advertising is the Looking Glass slice
- [x] LoRa/packet-radio deployment docs (power/bandwidth budgets) — `docs/deployment-lora.md`: operator budget guide grounded in the real reticulum consts (`packet::MTU`=500, HEADER_2 budget 465, `MAX_TUNNEL_PAYLOAD`=384, `DEFAULT_BATCH_BUDGET`=441 = 465−24 cipher reserve, the milli-byte `TokenBucket`, `max_batch_age_ms`, flood-fill `ttl_hops`, `DEFAULT_ESTABLISHMENT_TIMEOUT_MS`=6000) with a worked SFxBW→bytes/sec→airtime-per-batch table, EU868 1%-duty governor tuning, mesh/store-and-forward topology, and airtime-as-power guidance. RabbitHole facts vs illustrative PHY numbers rigorously separated (per-table caveats + a Provenance section); surfaces a real gap: at SF12/SF11 the EU 1% budget falls below the bucket's 1 B/s integer refill floor. Framed as forward-looking (RNS transport is model-only until the adapter lands). Linked from `docs/deployment.md`

---

## Continuous tracks (every wave)

- [~] Protocol spec kept in lockstep with implementation (`docs/protocol/`) — full drift audit vs `crates/proto` (7 mismatches fixed incl. the file.md BulkPreamble mis-typed as frame 30, missing session personas/TOTP/Register groups, presence directory 10–13, blob 100–103); new `federation.md` (S2S wire + catalog sync + attestation, wire-today vs model-only marked) and `docs/legacy-surfaces.md` (12-surface operator matrix: ports, toggles, min-roles, rate classes, deferrals); README family table (RADIO 9 reserved, FEDERATION 8 S2S-only). Ongoing discipline: re-audit with each proto change
- [~] Golden-file + fuzz tests accompany every codec — audited all 12 wire codecs; hotline/ftn/zmodem/icecast/syndication already had both golden + fuzz (left alone). Added **30 tests** filling gaps: `swarm` (pinned `Manifest`/`CapToken` postcard wire images + decoder totality), `legacy-binkp` (M_NUL/M_ADR/M_FILE frames + stream walk), `legacy-qwk` (full MESSAGES.DAT image, every offset pinned), `legacy-nntp` (OVER line + framed datablock + dot-stuffing + seeded sweep), `legacy-telnet`/`legacy-finger`/`legacy-doors` (IAC/query/DOOR.SYS goldens + totality sweeps) — each golden asserts both directions (decode + re-encode fixed point). **Also fixed a real remotely-triggerable DoS** the audit found: `legacy-finger::parse_query` panicked on a leading multi-byte UTF-8 char (the server's `from_utf8_lossy` turns a `0xFF` line into a leading U+FFFD; the old `rest[..2]` sliced mid-char) — now compares the first two *bytes*, total over arbitrary input; regression + arbitrary-byte sweep added. Remaining: `cargo-fuzz` harnesses (nightly) as an optional CI addon
- [x] CHANGELOG + semver discipline on `proto` — `proto::registry` (`REGISTRY`: all 174 `(Family, u16, name)` triples across 9 active families — SESSION 34, PRESENCE 16, CHAT 22, DM 9, BOARD 14, FILE 33, SWARM 8, ADMIN 31, WISH 7; FEDERATION/RADIO reserved) built from each type's own `Message::FAMILY`/`MESSAGE_TYPE` (rename → won't compile, renumber → golden shifts) with 3 guard tests: (family,type) uniqueness, `EXPECTED=174` completeness tripwire, and a checked-in `wire-registry.golden` re-blessed via `BLESS=1`. `docs/protocol/versioning.md` (additive-only-within-version policy, `#[non_exhaustive]` discipline, negotiation) + root `CHANGELOG.md` (Keep-a-Changelog, 0.63.0→foundations)
- [ ] Mobile cross-compile smoke in CI from W0 (front-load NDK pain)
