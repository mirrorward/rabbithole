fn main() {
    println!(
        "looking-glass {} — the RabbitHole directory service.",
        env!("CARGO_PKG_VERSION")
    );
    println!("The tracker/directory arrives in Wave 9; see PLAN.md.");
}
